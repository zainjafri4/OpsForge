export const awsAdvancedQuestions = [
  // ─── EASY THEORY ───────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the primary difference between RDS Multi-AZ and Read Replicas?',
    options: [
      'Multi-AZ provides synchronous replication for high availability; Read Replicas provide asynchronous replication for read scalability',
      'Multi-AZ is cheaper than Read Replicas',
      'Read Replicas provide automatic failover; Multi-AZ does not which is a fundamental architectural constraint which is a fundamental architectural constraint',
      'Multi-AZ can be in different regions; Read Replicas cannot',
    ],
    correctIndex: 0,
    explanation: 'RDS Multi-AZ deployments provide high availability and durability through synchronous replication to a standby instance in a different Availability Zone. In case of infrastructure failure, RDS automatically fails over to the standby. This happens transparently with no manual intervention, typically within 60-120 seconds. Read Replicas, on the other hand, use asynchronous replication and are designed to offload read traffic from the primary database. They can be in the same region or cross-region. Read Replicas do not provide automatic failover — they are primarily for scaling read operations. Multi-AZ costs roughly double because you pay for the standby instance, but it is essential for production workloads requiring high availability. Read Replicas can be promoted to standalone databases manually if needed.',
    tags: ['aws', 'rds', 'multi-az', 'read-replica', 'high-availability'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What triggers an AWS Lambda function execution?',
    options: [
      'Event sources such as S3, API Gateway, DynamoDB Streams, SNS, SQS, EventBridge, or direct invocation',
      'Only HTTP requests via API Gateway using standard configuration methods as documented in official AWS best practices',
      'Only scheduled CloudWatch Events as documented in official AWS best practices according to industry standard guidelines',
      'Lambda functions run continuously like EC2 instances which is the standard recommended approach according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'AWS Lambda is an event-driven compute service that executes code in response to triggers from various AWS services. Common event sources include: S3 (object creation/deletion), API Gateway (HTTP requests), DynamoDB Streams (table changes), SNS topics, SQS queues, EventBridge (scheduled or custom events), Kinesis streams, CloudWatch Logs, Cognito, IoT, Step Functions, and direct SDK/CLI invocation. Lambda functions are stateless and ephemeral — they do not run continuously. Each invocation starts a new execution environment (cold start) or reuses a warm one. The function runs only for the duration needed to process the event, and you pay only for compute time consumed. This makes Lambda ideal for sporadic workloads, microservices, and event-driven architectures. Understanding event source mappings is critical for serverless architecture design.',
    tags: ['aws', 'lambda', 'serverless', 'event-driven', 'triggers'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the purpose of Amazon CloudFront and how does it improve application performance?',
    options: [
      'CloudFront is a CDN that caches content at edge locations worldwide, reducing latency for end users',
      'CloudFront is a load balancer for EC2 instances which is the standard recommended approach using standard configuration methods',
      'CloudFront is a DNS service like Route 53 following common design principles based on standard operational procedures',
      'CloudFront is a database caching layer as recommended for high availability which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'Amazon CloudFront is a Content Delivery Network (CDN) with over 400 edge locations globally. It caches copies of your content (static files, videos, API responses) at these edge locations, bringing data closer to end users geographically. When a user requests content, CloudFront routes the request to the nearest edge location. If the content is cached (cache hit), it is served immediately with low latency. If not (cache miss), CloudFront fetches it from the origin (S3, ELB, or custom HTTP server), caches it, and serves it. CloudFront significantly reduces latency, decreases load on your origin servers, and provides DDoS protection via AWS Shield. It also supports SSL/TLS encryption, custom domain names, and Lambda@Edge for edge computing. Typical use cases: serving static website assets, streaming video content, accelerating APIs.',
    tags: ['aws', 'cloudfront', 'cdn', 'edge', 'performance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon EKS and why would you choose it over self-managed Kubernetes on EC2?',
    options: [
      'EKS is a managed Kubernetes service; AWS handles control plane upgrades, patching, and high availability',
      'EKS is cheaper than self-managed Kubernetes',
      'EKS does not require any EC2 instances',
      'EKS only works with AWS services and cannot run standard Kubernetes workloads and this applies to all deployment scenarios',
    ],
    correctIndex: 0,
    explanation: 'Amazon Elastic Kubernetes Service (EKS) is a fully managed Kubernetes service where AWS operates and maintains the Kubernetes control plane (API server, etcd, scheduler, controller manager). You deploy worker nodes (EC2 instances or Fargate pods) but AWS handles control plane scaling, patching, and multi-AZ redundancy. Benefits: reduced operational overhead, automatic Kubernetes version upgrades, integrated AWS security (IAM for RBAC), deep integration with VPC networking, ELB, CloudWatch, and IAM Roles for Service Accounts (IRSA). EKS runs upstream Kubernetes, so all standard Kubernetes manifests, Helm charts, and tooling work without modification. The trade-off: EKS control plane costs $0.10/hour per cluster (~$73/month) regardless of usage. For production workloads, EKS is preferred because self-managing Kubernetes control planes is complex and error-prone.',
    tags: ['aws', 'eks', 'kubernetes', 'managed-service', 'containers'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'SCENARIO' as const,
    question: 'Your web application experiences a sudden traffic spike. The RDS database becomes a bottleneck with read queries taking over 10 seconds. What is the quickest solution to offload read traffic?',
    options: [
      'Create an RDS Read Replica and direct read traffic to it via a read endpoint',
      'Upgrade the RDS instance to a larger instance type which is the standard recommended approach',
      'Enable RDS Multi-AZ for better performance',
      'Switch to DynamoDB immediately',
    ],
    correctIndex: 0,
    explanation: 'Creating an RDS Read Replica is the fastest and most cost-effective way to scale read-heavy workloads. A Read Replica is an asynchronous copy of the database that handles SELECT queries, offloading traffic from the primary instance. You can create a replica in minutes and immediately route read traffic to it using its endpoint. Your application must be configured to use different endpoints for reads and writes (this is often done via database connection pooling or a proxy like ProxySQL). Upgrading the instance type (B) requires downtime and may not fully resolve the issue if reads overwhelm a single instance. Multi-AZ (C) does not improve performance — it is for high availability only, and the standby is not accessible for reads. Migrating to DynamoDB (D) is a massive architectural change requiring code rewrites. Read Replicas can also be used for disaster recovery, analytics workloads, and cross-region replication.',
    tags: ['aws', 'rds', 'read-replica', 'scalability', 'performance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'SCENARIO' as const,
    question: 'Your Lambda function is timing out when processing large S3 files. The default timeout is 3 seconds. What should you do?',
    options: [
      'Increase the Lambda timeout configuration (max 15 minutes) and allocate more memory if needed',
      'Lambda cannot process large files; use EC2 instead',
      'Split the S3 file into smaller chunks before uploading as documented in the official best practices',
      'Lambda automatically scales timeout based on file size',
    ],
    correctIndex: 0,
    explanation: 'AWS Lambda allows you to configure the timeout from 1 second to 15 minutes (900 seconds). If your function is timing out, the first step is to increase the timeout value in the Lambda configuration. Additionally, Lambda memory allocation affects CPU performance — Lambda allocates proportional CPU power based on memory. If you allocate 128 MB, you get minimal CPU; if you allocate 1792 MB (the maximum), you get approximately 1 full vCPU. For CPU-intensive tasks like file parsing or image processing, increasing memory improves execution speed. If processing still exceeds 15 minutes, you should redesign using Step Functions to orchestrate multiple Lambda invocations, or use ECS/Fargate for long-running tasks. Lambda is not inherently unsuitable for large files (B is wrong) — it depends on the processing complexity. Splitting files (C) may help but is not always necessary. Lambda does not auto-adjust timeout (D).',
    tags: ['aws', 'lambda', 'timeout', 'performance', 'troubleshooting'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'SCENARIO' as const,
    question: 'You need to serve a static website hosted on S3 with a custom domain and HTTPS. What AWS services do you need?',
    options: [
      'S3 for hosting, CloudFront for CDN and HTTPS, Route 53 for DNS, ACM for SSL certificate',
      'S3 and Route 53 only (S3 supports HTTPS natively) and this applies to all deployment scenarios',
      'EC2 with Nginx, Route 53, and ACM',
      'S3, API Gateway, and ACM',
    ],
    correctIndex: 0,
    explanation: 'To serve a static website with a custom domain and HTTPS, you need: (1) S3 bucket configured for static website hosting (contains HTML/CSS/JS files), (2) CloudFront distribution pointing to the S3 bucket as the origin (CloudFront provides HTTPS and global edge caching), (3) AWS Certificate Manager (ACM) certificate for your domain (free SSL/TLS certificate, must be in us-east-1 region for CloudFront), (4) Route 53 hosted zone with an A record (alias) pointing to the CloudFront distribution. S3 alone does not support custom domains with HTTPS (B is incorrect) — S3 website endpoints do not support HTTPS. EC2 with Nginx (C) is overkill and introduces unnecessary operational complexity. API Gateway (D) is for APIs, not static websites. This architecture is extremely common for modern web applications and is a standard interview question.',
    tags: ['aws', 's3', 'cloudfront', 'route53', 'acm', 'static-website'],
  },

  // ─── MEDIUM THEORY ─────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are Lambda cold starts and how can you minimize their impact?',
    options: [
      'Cold starts occur when Lambda initializes a new execution environment; mitigate with Provisioned Concurrency, smaller deployment packages, and keeping functions warm',
      'Cold starts are network delays; use VPC endpoints to fix them',
      'Cold starts happen only with interpreted languages like Python; use compiled languages like Go and this applies to all deployment scenarios and this applies to all deployment scenarios',
      'Cold starts are unavoidable and cannot be mitigated',
    ],
    correctIndex: 0,
    explanation: 'A cold start occurs when AWS Lambda must initialize a new execution environment because no warm environment is available. This involves: (1) downloading your deployment package, (2) starting a runtime (Node.js, Python, etc.), (3) executing initialization code outside the handler (imports, connections). Cold starts add 100ms-3s latency depending on package size, runtime, and whether the function is in a VPC. Mitigation strategies: (1) Provisioned Concurrency — keeps N execution environments initialized and warm, eliminating cold starts for those invocations (costs ~$0.015/hour per provisioned instance), (2) reduce deployment package size (tree-shake dependencies, use Webpack/esbuild), (3) avoid VPCs unless necessary (VPC ENI setup adds latency), (4) use lighter runtimes (Node.js/Python are faster to start than Java/.NET), (5) keep functions "warm" with scheduled pings (not recommended by AWS anymore). Compiled languages (C) do not eliminate cold starts, though they may reduce initialization time slightly. Cold starts are inherent to serverless but can be managed.',
    tags: ['aws', 'lambda', 'cold-start', 'performance', 'provisioned-concurrency'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon Aurora Serverless and when should you use it over standard Aurora?',
    options: [
      'Aurora Serverless auto-scales capacity based on demand and pauses during inactivity; ideal for intermittent or unpredictable workloads',
      'Aurora Serverless is cheaper than standard Aurora for all workloads as documented in the official best practices as documented in the official best practices',
      'Aurora Serverless provides higher performance than standard Aurora',
      'Aurora Serverless does not require a VPC',
    ],
    correctIndex: 0,
    explanation: 'Amazon Aurora Serverless is an on-demand, auto-scaling configuration for Aurora (PostgreSQL or MySQL). Instead of provisioning fixed instance sizes (db.r5.large, etc.), you define capacity in Aurora Capacity Units (ACUs). Aurora automatically scales compute and memory from min to max ACU settings based on load. When there is no activity, Aurora can pause the database entirely, and you pay only for storage. This is ideal for: (1) development/test databases (pause during nights/weekends), (2) infrequent or unpredictable workloads (e.g., internal tools used sporadically), (3) applications with variable traffic. However, Aurora Serverless has limitations: higher latency during scaling events (5-15 seconds), no read replicas, no cross-region replication, not suitable for consistently high-traffic production workloads. For steady, predictable production workloads, standard Aurora with fixed instance sizes is more cost-effective and performant. Aurora Serverless v2 (released 2022) improves scaling speed but costs more per ACU.',
    tags: ['aws', 'aurora', 'serverless', 'auto-scaling', 'rds'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do CloudFront signed URLs differ from signed cookies, and when would you use each?',
    options: [
      'Signed URLs restrict access to individual files; signed cookies restrict access to multiple files or patterns (e.g., all files in /premium/)',
      'Signed URLs are for public content; signed cookies are for private content which is the standard recommended approach which is the standard recommended approach',
      'Signed URLs use S3 pre-signed URLs; signed cookies use CloudFront',
      'Signed URLs expire after 1 hour; signed cookies never expire',
    ],
    correctIndex: 0,
    explanation: 'CloudFront signed URLs and signed cookies both provide temporary, secure access to private content served via CloudFront. Signed URLs embed access permissions and expiration directly in the URL query string (e.g., ?Expires=...&Signature=...). Use signed URLs when: (1) you want to restrict access to a single file (e.g., download a paid PDF), (2) users cannot handle cookies (RTMP streaming), (3) the URL is embedded in an email or third-party app. Signed cookies store access permissions in HTTP cookies set by your application. Use signed cookies when: (1) you want to provide access to multiple files (e.g., all videos in a premium course directory), (2) you want to avoid changing URLs, (3) the application manages sessions (e.g., after user login, set a signed cookie for /premium/*). Both require a trusted signer (CloudFront key pair or AWS Secrets Manager). Signed URLs and cookies are not related to S3 pre-signed URLs (C is wrong) — those are generated by S3 directly and bypass CloudFront.',
    tags: ['aws', 'cloudfront', 'signed-urls', 'signed-cookies', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the purpose of RDS Proxy and how does it improve database connections?',
    options: [
      'RDS Proxy pools and reuses database connections, reducing connection overhead and improving scalability for serverless architectures',
      'RDS Proxy provides a public endpoint for private RDS databases which is the standard recommended approach which is the standard recommended approach',
      'RDS Proxy caches query results to improve performance',
      'RDS Proxy automatically backs up the database',
    ],
    correctIndex: 0,
    explanation: 'Amazon RDS Proxy is a fully managed database proxy that sits between your application and RDS/Aurora database. It maintains a pool of established database connections and multiplexes application connections to this pool. Benefits: (1) reduces database connection overhead — Lambda functions and other serverless apps create thousands of short-lived connections, overwhelming the database; RDS Proxy reuses connections efficiently, (2) improves failover times — during RDS Multi-AZ failover, RDS Proxy preserves connections and automatically routes traffic to the new primary, reducing failover from 60s to 20s, (3) enforces IAM authentication for database access without embedding credentials. RDS Proxy does NOT provide public endpoints (B) — you still need secure networking (VPC, security groups). It does NOT cache query results (C) — for that, use ElastiCache. It does NOT handle backups (D). RDS Proxy is particularly valuable for Lambda-to-RDS architectures where connection storms can exhaust database connections. It costs ~$0.015/hour per vCPU plus data transfer.',
    tags: ['aws', 'rds-proxy', 'lambda', 'connection-pooling', 'database'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your EKS cluster node group needs to scale based on pending pods that cannot be scheduled due to insufficient resources. What AWS feature should you use?',
    options: [
      'Cluster Autoscaler, which watches for unschedulable pods and automatically scales the node group',
      'Horizontal Pod Autoscaler (HPA), which scales pods based on CPU/memory which is the standard recommended approach',
      'Auto Scaling Groups with target tracking based on cluster CPU',
      'Karpenter, which provisions nodes dynamically',
    ],
    correctIndex: 0,
    explanation: 'The Cluster Autoscaler is a Kubernetes component that automatically adjusts the number of nodes in an EKS node group (backed by an EC2 Auto Scaling Group) based on pod scheduling needs. It watches for: (1) pods that cannot be scheduled due to insufficient resources (CPU/memory/GPU), (2) nodes that are underutilized and can be scaled down. When unschedulable pods are detected, Cluster Autoscaler increases the desired capacity of the ASG. When nodes are idle, it scales down. Cluster Autoscaler respects pod disruption budgets and node selectors. HPA (B) scales the number of pod replicas, not nodes. ASG target tracking (C) scales based on metrics like CPU but is not pod-aware. Karpenter (D) is an alternative to Cluster Autoscaler — it is a newer AWS-native autoscaler that provisions nodes more intelligently and efficiently, bypassing ASGs entirely. Karpenter is recommended for modern EKS setups but requires explicit adoption. Cluster Autoscaler is the default solution.',
    tags: ['aws', 'eks', 'kubernetes', 'cluster-autoscaler', 'auto-scaling'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your Lambda function accesses RDS in a private subnet. After deployment, the function times out when trying to connect to the database. What is likely wrong?',
    options: [
      'Lambda in a VPC requires a NAT Gateway or VPC endpoints for AWS service calls, and the security group must allow outbound traffic to RDS',
      'Lambda cannot access RDS in a private subnet',
      'You must enable Lambda Enhanced Networking',
      'RDS must be in a public subnet for Lambda to access it which is the standard recommended approach which is the standard recommended approach',
    ],
    correctIndex: 0,
    explanation: 'When a Lambda function is configured to run inside a VPC (to access private resources like RDS), it loses direct internet access. If the Lambda function needs to call AWS services (e.g., Secrets Manager to get DB credentials, or CloudWatch for logs), it must either: (1) have a route to a NAT Gateway in a public subnet, or (2) use VPC endpoints for AWS services (recommended for cost and performance). Additionally, the Lambda function security group must allow outbound traffic to the RDS security group on the database port (e.g., 3306 for MySQL, 5432 for PostgreSQL), and the RDS security group must allow inbound traffic from the Lambda security group. Common mistake: Lambda function is attached to a private subnet with no NAT Gateway, so it cannot initialize (cannot reach S3 for code download, cannot send logs to CloudWatch). Lambda CAN access RDS in private subnets (B is wrong). Enhanced Networking (C) is for EC2 high-throughput workloads. RDS should remain private (D is a security anti-pattern).',
    tags: ['aws', 'lambda', 'vpc', 'rds', 'networking', 'troubleshooting'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'You want to deploy a web application on EKS with a public-facing load balancer. Which Kubernetes Service type and AWS integration should you use?',
    options: [
      'Service type LoadBalancer with AWS Load Balancer Controller to provision an Application Load Balancer (ALB) or Network Load Balancer (NLB)',
      'Service type NodePort and expose nodes directly to the internet which is the standard recommended approach which is the standard recommended approach',
      'Service type ClusterIP and manually configure Route 53',
      'Use Ingress only without any Service',
    ],
    correctIndex: 0,
    explanation: 'For public-facing applications on EKS, you should use Kubernetes Service type LoadBalancer, which integrates with the AWS Load Balancer Controller (formerly ALB Ingress Controller). When you create a Service with type: LoadBalancer, the controller automatically provisions an AWS NLB (Network Load Balancer) or ALB (Application Load Balancer) depending on annotations. For HTTP/HTTPS traffic, use an Ingress resource with the ALB controller — it creates an ALB with target groups pointing to your pods. The controller manages ALB lifecycle, security groups, and listener rules. NodePort (B) exposes services on a static port on every node, requiring you to manually manage load balancing and security — not recommended for production. ClusterIP (C) is internal-only; external traffic cannot reach it without additional networking. Ingress alone (D) still requires a Service backend. Proper EKS load balancing architecture: Ingress (ALB) → Service (ClusterIP or NodePort) → Pods.',
    tags: ['aws', 'eks', 'kubernetes', 'load-balancer', 'alb', 'ingress'],
  },

  // ─── HARD THEORY ───────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'What is Lambda@Edge and how does it differ from CloudFront Functions?',
    options: [
      'Lambda@Edge runs Node.js/Python at edge locations with full Lambda capabilities; CloudFront Functions runs lightweight JavaScript with sub-millisecond latency but limited functionality',
      'Lambda@Edge is for S3 origins; CloudFront Functions is for custom origins using standard configuration methods following typical deployment workflows following common design principles following typical deployment workflows',
      'Lambda@Edge runs on the origin server; CloudFront Functions runs at edge locations which is the standard recommended approach which is the standard recommended approach based on production deployment experience',
      'Lambda@Edge is deprecated; use CloudFront Functions for all edge computing following common design principles as suggested by cloud architects based on standard operational procedures based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Both Lambda@Edge and CloudFront Functions enable edge computing — running code at CloudFront edge locations before requests reach the origin. Key differences: (1) Runtime: Lambda@Edge supports Node.js and Python with full AWS SDK access; CloudFront Functions only supports a restricted JavaScript subset. (2) Execution time: Lambda@Edge allows up to 5 seconds for viewer/origin requests; CloudFront Functions executes in under 1ms. (3) Use cases: Lambda@Edge is for complex logic (A/B testing with DynamoDB lookups, image resizing, auth token validation); CloudFront Functions is for simple, high-scale transformations (URL rewrites, header manipulation, request normalization). (4) Triggering events: Lambda@Edge supports viewer request/response + origin request/response; CloudFront Functions only supports viewer request/response. (5) Cost: CloudFront Functions is 1/6th the price. Neither is deprecated (D). Choose based on complexity: simple transformations → CloudFront Functions; complex logic or AWS service integration → Lambda@Edge.',
    tags: ['aws', 'lambda-edge', 'cloudfront-functions', 'edge-computing', 'cdn'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'How does Aurora Global Database provide cross-region disaster recovery, and what are the RPO/RTO characteristics?',
    options: [
      'Aurora Global Database replicates data asynchronously across regions with typical replication lag <1 second; RPO ~1 second, RTO <1 minute via manual promotion',
      'Aurora Global Database uses synchronous replication; RPO and RTO are both zero',
      'Aurora Global Database requires manual backup and restore; RPO and RTO depend on backup schedule which is the standard recommended approach which is the standard recommended approach',
      'Aurora Global Database is for read scaling only and does not support failover',
    ],
    correctIndex: 0,
    explanation: 'Aurora Global Database spans multiple AWS regions with one primary region (read-write) and up to five secondary regions (read-only). Physical replication occurs at the storage layer with typical lag under 1 second. In a disaster scenario where the primary region fails, you promote a secondary region to become the new primary. RPO (Recovery Point Objective) is typically ~1 second because replication lag is minimal. RTO (Recovery Time Objective) is under 1 minute for promotion, though application failover (DNS updates, connection rerouting) adds overhead. Synchronous cross-region replication (B) is impossible due to physics (speed of light latency between regions). Aurora Global does NOT require manual backup/restore (C) — it is real-time replication. It CAN be used for disaster recovery via promotion (D is incorrect) — secondary regions are not just for read scaling. Aurora Global is expensive (~2-3x the cost of single-region Aurora) but provides the best cross-region DR for RDS. Use cases: global applications, compliance (data residency), disaster recovery for critical databases.',
    tags: ['aws', 'aurora', 'global-database', 'disaster-recovery', 'rpo', 'rto'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'What is the purpose of EKS IAM Roles for Service Accounts (IRSA) and how does it improve security?',
    options: [
      'IRSA maps Kubernetes service accounts to IAM roles, allowing pods to assume IAM permissions without sharing instance roles or storing credentials',
      'IRSA provides SSH access to EKS worker nodes which is the standard recommended approach which is the standard recommended approach which is the standard recommended approach',
      'IRSA encrypts secrets in etcd',
      'IRSA is a replacement for Kubernetes RBAC',
    ],
    correctIndex: 0,
    explanation: 'EKS IAM Roles for Service Accounts (IRSA) allows fine-grained IAM permissions for individual Kubernetes pods by mapping a Kubernetes ServiceAccount to an AWS IAM role. Without IRSA, all pods on a node share the IAM role attached to the EC2 instance, violating the principle of least privilege. With IRSA: (1) each pod can have its own IAM role, (2) pods obtain temporary credentials via the IAM OpenID Connect (OIDC) provider associated with the EKS cluster, (3) credentials are automatically rotated and injected via environment variables (AWS_ROLE_ARN, AWS_WEB_IDENTITY_TOKEN_FILE). This eliminates the need to store AWS credentials in Kubernetes Secrets or ConfigMaps. Example: a pod running an application that writes to S3 can assume an IAM role with s3:PutObject permissions, while another pod on the same node has no S3 access. IRSA does NOT provide SSH access (B), does NOT encrypt secrets in etcd (C) — use KMS encryption for that, and does NOT replace RBAC (D) — IRSA handles AWS permissions, RBAC handles Kubernetes permissions.',
    tags: ['aws', 'eks', 'irsa', 'iam', 'service-accounts', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'How does RDS Performance Insights help diagnose database performance issues, and what is a "wait event"?',
    options: [
      'Performance Insights visualizes database load and identifies bottlenecks by showing wait events — the resources (CPU, I/O, locks) that queries are waiting for',
      'Performance Insights automatically optimizes slow queries using conventional setup approaches using standard configuration methods and this approach is commonly used in enterprise settings',
      'Performance Insights is a log aggregation tool for RDS error logs which is the standard recommended approach which is the standard recommended approach following the recommended security practices',
      'Performance Insights provides real-time alerts for high CPU usage following common design principles following typical deployment workflows as recommended for high availability',
    ],
    correctIndex: 0,
    explanation: 'RDS Performance Insights is a database performance monitoring tool that shows database load over time, broken down by wait events. A "wait event" is a point in query execution where the database engine must wait for a resource before proceeding. Common wait events: (1) CPU — query execution is CPU-bound, (2) IO:XactSync — waiting for transaction commit to flush to disk (high fsync latency), (3) Lock:transactionid — waiting for a row-level lock held by another transaction, (4) IO:DataFileRead — waiting for disk reads (missing indexes or cold cache). Performance Insights displays: (1) Database Load graph (Average Active Sessions over time), (2) Top SQL queries contributing to load, (3) Wait events for each query, (4) Dimensions (users, hosts, databases). It does NOT auto-optimize queries (B) — that requires manual tuning or AWS Trusted Advisor. It does NOT aggregate logs (C). It does NOT send alerts (D) — use CloudWatch Alarms for that. Performance Insights is critical for diagnosing sudden performance degradation.',
    tags: ['aws', 'rds', 'performance-insights', 'wait-events', 'database', 'monitoring'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your EKS cluster is experiencing intermittent pod scheduling failures with "Insufficient CPU" errors, but cluster CPU utilization is only 40%. What could be the cause?',
    options: [
      'Pods have high CPU requests (not limits), causing resource fragmentation; nodes cannot fit new pods even though overall cluster utilization is low',
      'EKS has a bug; restart the cluster following the recommended security practices implementing typical operational patterns following common design principles',
      'Kubernetes always shows incorrect CPU metrics as documented in the official best practices as documented in the official best practices which follows AWS recommended architecture patterns',
      'The Cluster Autoscaler is disabled according to industry standard guidelines as documented in official AWS best practices as recommended for high availability',
    ],
    correctIndex: 0,
    explanation: 'This is a classic resource fragmentation problem caused by improper pod resource requests and limits. Kubernetes schedules pods based on resource requests (not limits). If pods have very high CPU/memory requests but low actual usage, nodes may appear underutilized but cannot fit additional pods because their allocatable resources are "reserved" by existing pod requests. Example: 4-core node with 3.5 cores allocatable. If 5 pods each request 1 core but use only 0.2 cores, the node shows 60% utilization but cannot schedule a new pod requesting 1 core (only 0.5 cores remain allocatable). Solutions: (1) right-size pod requests to match actual usage (use Vertical Pod Autoscaler or metrics analysis), (2) use Cluster Autoscaler to add nodes when scheduling fails, (3) use Descheduler to rebalance pods, (4) enable bin-packing node scoring. Restarting the cluster (B) does nothing. Kubernetes metrics are accurate (C is wrong). Cluster Autoscaler (D) would help but does not address the root cause. This question tests deep understanding of Kubernetes scheduling.',
    tags: ['aws', 'eks', 'kubernetes', 'resource-requests', 'scheduling', 'troubleshooting'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'You are implementing a blue-green deployment for an EKS application using two separate Kubernetes Deployments. How can you switch traffic between blue and green versions with zero downtime?',
    options: [
      'Update the Kubernetes Service selector to point to the green Deployment labels, then delete the blue Deployment after validation',
      'Delete the blue Deployment and immediately create the green Deployment which is the standard recommended approach which is the standard recommended approach',
      'Use kubectl rolling-update command',
      'Change the Docker image tag in the blue Deployment',
    ],
    correctIndex: 0,
    explanation: `Blue-green deployment in Kubernetes involves running two separate Deployments (blue and green) with different label selectors. A Kubernetes Service routes traffic based on its selector. Initial state: Service selector matches blue Deployment (app=myapp, version=blue). To switch: (1) Deploy green Deployment (app=myapp, version=green) with the new version. (2) Validate green Deployment is healthy (all pods running, health checks passing). (3) Update Service selector to match green (kubectl patch service myapp -p '{"spec":{"selector":{"version":"green"}}}') — traffic instantly switches with zero dropped connections. (4) Monitor green in production. (5) If issues occur, rollback by patching the Service selector back to blue. (6) Once confident, delete blue Deployment. This provides instant switchover and easy rollback. Deleting blue before green is ready (B) causes downtime. kubectl rolling-update (C) is deprecated and does not apply here. Changing the image tag (D) is a rolling update, not blue-green. This pattern is common in production CI/CD pipelines.`,
    tags: ['aws', 'eks', 'kubernetes', 'blue-green', 'deployment', 'zero-downtime'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your Aurora database experiences a sudden 10x increase in IOPS due to a batch analytics job, causing production queries to slow down. How can you isolate the analytics workload?',
    options: [
      'Create an Aurora Read Replica with a custom endpoint and direct analytics queries there; use query priorities or separate connections',
      'Increase Aurora instance size to handle both workloads which is the standard recommended approach which is the standard recommended approach',
      'Enable Aurora Multi-AZ to distribute load',
      'Use RDS Proxy to throttle the analytics queries',
    ],
    correctIndex: 0,
    explanation: 'Aurora Read Replicas can be used for workload isolation. Create a dedicated Read Replica (or multiple replicas) and configure your analytics application to use the replica endpoint. This offloads heavy read IOPS from the primary instance, protecting production write and latency-sensitive read traffic. Aurora also supports Custom Endpoints — you can group multiple replicas into a custom endpoint and route specific workload types (e.g., analytics vs. application queries) to different endpoints. Additionally, you can use Aurora priority queues (connection-level session priorities) to deprioritize low-priority queries when the database is under load. Increasing instance size (B) is expensive and does not isolate workloads — both still compete for resources. Multi-AZ (C) is for high availability, not performance isolation. RDS Proxy (D) does connection pooling but does not throttle or prioritize queries. Proper workload isolation is a critical pattern for production databases serving multiple use cases (OLTP + OLAP).',
    tags: ['aws', 'aurora', 'read-replica', 'workload-isolation', 'performance', 'database'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your Lambda function processes images uploaded to S3. During peak hours, thousands of images are uploaded simultaneously, causing Lambda throttling errors. What is the best solution?',
    options: [
      'Use an SQS queue between S3 and Lambda; configure S3 to send events to SQS, then trigger Lambda from SQS with controlled concurrency',
      'Increase Lambda reserved concurrency to handle all requests as documented in the official best practices as documented in the official best practices',
      'Use Step Functions to orchestrate Lambda invocations',
      'Switch to EC2 instances for image processing',
    ],
    correctIndex: 0,
    explanation: 'Lambda concurrent execution limits are per region (default 1000, can be increased via quota request). If your S3 bucket receives 5000 uploads simultaneously and triggers Lambda directly, you will hit throttling errors (TooManyRequestsException). The best solution is to decouple S3 and Lambda using an SQS queue: (1) Configure S3 Event Notifications to send ObjectCreated events to SQS, (2) Configure Lambda to poll SQS with a controlled batch size (e.g., 10 messages per invocation) and reserved concurrency (e.g., 100 concurrent Lambda executions), (3) Lambda processes images from the queue at a controlled rate. SQS buffers bursts and Lambda processes messages as capacity allows. This prevents throttling, provides retry logic via SQS visibility timeout, and enables DLQ for failed messages. Increasing reserved concurrency (B) is not scalable and may exhaust account-level concurrency. Step Functions (C) is for orchestration, not burst buffering. Switching to EC2 (D) increases operational complexity. SQS as a buffer is a standard serverless pattern.',
    tags: ['aws', 'lambda', 'sqs', 'throttling', 's3', 'event-driven', 'queue'],
  },

  // ─── ADDITIONAL AWS ADVANCED QUESTIONS ───────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are AWS Graviton processors and when should you consider using them?',
    options: [
      'Graviton processors are AWS-designed ARM-based chips offering up to 40% better price-performance than x86; use them for containerized workloads, web servers, and applications without x86-specific dependencies',
      'Graviton is only for machine learning workloads and this applies to all deployment scenarios and this applies to all deployment scenarios and this applies to all deployment scenarios as suggested by cloud architects as documented in official AWS best practices',
      'Graviton instances are more expensive than x86 following the recommended security practices as suggested by cloud architects using standard configuration methods using established implementation patterns using standard configuration methods',
      'Graviton only works with Windows workloads as suggested by cloud architects according to industry standard guidelines implementing typical operational patterns following the recommended security practices as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'AWS Graviton processors are custom ARM-based chips designed by AWS. Versions: Graviton2 (2020), Graviton3 (2022), Graviton4 (2024). Benefits: (1) Price-performance: up to 40% better than comparable x86 instances for many workloads. (2) Energy efficiency: lower power consumption. (3) Consistent performance: no noisy neighbor issues (dedicated hardware performance). Instance families: T4g, M6g, C6g, R6g, and newer (g suffix = Graviton). Good candidates: (1) Containerized workloads (EKS, ECS). (2) Web servers (Nginx, Apache). (3) Caching (Redis, Memcached). (4) Java applications (well-optimized for ARM). (5) .NET Core, Python, Node.js applications. Considerations: (1) Must use ARM-compatible binaries/containers. (2) Some software may lack ARM support. (3) Test thoroughly before migrating production. Migration: use multi-arch Docker images (docker buildx for ARM64), test with Graviton instances in dev/staging. Graviton is general-purpose (B). Graviton is cheaper (C). Graviton runs Linux, not Windows (D).',
    tags: ['aws', 'graviton', 'arm', 'cost-optimization', 'ec2', 'performance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'What is AWS Control Tower and how does it help manage multi-account AWS environments?',
    options: [
      'Control Tower automates the setup of a multi-account environment with landing zones, guardrails (preventive and detective), Account Factory for provisioning, and centralized logging/security',
      'Control Tower is a database migration service following common design principles as documented in official AWS best practices implementing typical operational patterns as documented in official AWS best practices',
      'Control Tower is only for single-account setups and this applies to all deployment scenarios and this applies to all deployment scenarios and this applies to all deployment scenarios as documented in official AWS best practices',
      'Control Tower replaces IAM according to industry standard guidelines which follows AWS recommended architecture patterns as documented in official AWS best practices implementing typical operational patterns',
    ],
    correctIndex: 0,
    explanation: 'AWS Control Tower is a service for setting up and governing secure multi-account AWS environments. Key features: (1) Landing Zone: pre-configured multi-account environment with best practices — organizational units (Security, Sandbox, Workloads), core accounts (Log Archive, Audit), and foundational infrastructure. (2) Guardrails: governance rules applied at OU level. Preventive: SCPs that block non-compliant actions. Detective: AWS Config rules that detect and alert on drift. (3) Account Factory: self-service provisioning of new AWS accounts with pre-configured guardrails, VPC, IAM, and SSO. (4) Dashboard: centralized compliance status across all accounts. Use cases: (1) Enterprise multi-account strategy. (2) Compliance (HIPAA, PCI, SOC). (3) Standardized account provisioning. (4) Centralized logging and security. Integration: works with Organizations, Service Catalog, SSO, CloudTrail, Config. Control Tower is governance, not migration (B). It is for multi-account (C). It complements, not replaces, IAM (D).',
    tags: ['aws', 'control-tower', 'multi-account', 'governance', 'landing-zone', 'guardrails'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your application uses Lambda functions that need to access resources in a VPC (RDS, ElastiCache). After enabling VPC access, Lambda functions take 10+ seconds to start. How do you fix this?',
    options: [
      'Use Lambda Provisioned Concurrency to keep warm execution environments, or accept the cold start latency and optimize function initialization',
      'Disable VPC access for Lambda based on standard operational procedures according to industry standard guidelines which follows AWS recommended architecture patterns',
      'Lambda cannot access VPC resources and this approach is commonly used in enterprise settings as documented in official AWS best practices as recommended for high availability',
      'Increase Lambda memory to reduce cold starts which is the standard recommended approach which is the standard recommended approach implementing typical operational patterns',
    ],
    correctIndex: 0,
    explanation: 'Lambda VPC cold starts: when Lambda runs in a VPC, AWS must attach an Elastic Network Interface (ENI) to the execution environment. This used to cause 10+ second cold starts. AWS improvements (2019+): Hyperplane ENI architecture dramatically reduced VPC cold starts to milliseconds (similar to non-VPC Lambda). If you still see slow cold starts: (1) Ensure youre on the latest Lambda runtime and not using legacy VPC networking. (2) VPC endpoints can reduce latency for AWS service calls. Solutions for latency-sensitive applications: (1) Provisioned Concurrency: keep N execution environments warm (eliminates cold starts entirely). Billed per hour for provisioned capacity. (2) Optimize function initialization: move expensive operations (DB connections, SDK initialization) outside the handler. (3) Use smaller deployment packages. (4) Choose appropriate memory (more memory = more CPU). Disabling VPC (B) is not an option if you need VPC resources. Lambda can access VPC resources (C). Memory helps but does not eliminate cold starts (D).',
    tags: ['aws', 'lambda', 'vpc', 'cold-start', 'provisioned-concurrency', 'performance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'You need to implement a data lake on AWS that supports both SQL queries and machine learning workloads. Which services and architecture would you use?',
    options: [
      'Use S3 as storage, AWS Glue for ETL and Data Catalog, Athena for SQL queries, Lake Formation for governance, and SageMaker for ML — all reading from the same S3 data lake',
      'Use only RDS for all data storage',
      'Data lakes require on-premises Hadoop clusters',
      'Use separate copies of data for analytics and ML which is the standard recommended approach which is the standard recommended approach which is the standard recommended approach',
    ],
    correctIndex: 0,
    explanation: 'Modern AWS data lake architecture: Storage: S3 as the central data lake (cheap, scalable, durable). Use partitioning, Parquet/ORC formats for query performance. Data ingestion: Kinesis Data Firehose for streaming, AWS DMS for database migration, Glue for batch ETL. Data Catalog: AWS Glue Data Catalog (Hive-compatible metastore) — stores table definitions, schema, partitions. Shared by Athena, Redshift, EMR, SageMaker. Query engines: (1) Athena: serverless SQL on S3 (ad-hoc queries, pay-per-query). (2) Redshift Spectrum: query S3 from Redshift (combined with DW data). (3) EMR: Spark/Hive for complex transformations. Machine Learning: SageMaker reads directly from S3 or queries via Athena. Feature Store can materialize features from the data lake. Governance: Lake Formation provides fine-grained access control, column-level security, data lineage. Benefits: single source of truth, schema-on-read flexibility, cost-effective storage. RDS alone (B) cannot handle data lake scale. Hadoop (C) is not required. Duplicating data (D) is inefficient.',
    tags: ['aws', 'data-lake', 's3', 'athena', 'glue', 'sagemaker', 'lake-formation'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is AWS App Runner and when would you choose it over ECS or EKS?',
    options: [
      'App Runner is a fully managed container service that automatically builds, deploys, and scales web apps from source code or container images — choose it for simplicity when you do not need Kubernetes/ECS features',
      'App Runner is only for static websites and this applies to all deployment scenarios and this applies to all deployment scenarios and this applies to all deployment scenarios based on production deployment experience as suggested by cloud architects',
      'App Runner is more complex than EKS and this approach is commonly used in enterprise settings implementing typical operational patterns following common design principles which follows AWS recommended architecture patterns',
      'App Runner does not support containers as recommended for high availability as suggested by cloud architects and this approach is commonly used in enterprise settings as recommended for high availability using established implementation patterns',
    ],
    correctIndex: 0,
    explanation: 'AWS App Runner is a fully managed service for running containerized web applications: Features: (1) Deploy from source (GitHub) or container image (ECR). (2) Automatic builds, deployments, scaling, load balancing, TLS. (3) No infrastructure management — no VPCs, clusters, or tasks to configure. (4) Pay per use (vCPU/memory-seconds when processing requests). When to use App Runner: (1) Simple web applications without complex networking. (2) Teams without Kubernetes/container expertise. (3) Rapid prototyping and MVPs. (4) Startups wanting to minimize ops overhead. When to use ECS/EKS instead: (1) Complex networking requirements (VPCs, service mesh). (2) Fine-grained control over scaling, placement, resources. (3) Multi-container pods (EKS). (4) Existing Kubernetes workloads. (5) Hybrid cloud with Kubernetes. App Runner trades flexibility for simplicity. It supports web services (B). It is simpler than EKS (C). It runs containers (D).',
    tags: ['aws', 'app-runner', 'containers', 'serverless', 'eks', 'ecs', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your organization requires all inter-service communication within AWS to be encrypted and authenticated. How do you implement this for EKS workloads?',
    options: [
      'Deploy a service mesh (Istio or AWS App Mesh) that provides automatic mTLS between services, encrypting all traffic and verifying service identity',
      'Use security groups to encrypt traffic using standard configuration methods following typical deployment workflows as documented in official AWS best practices',
      'HTTPS is sufficient for all internal traffic as documented in the official best practices as documented in the official best practices as documented in official AWS best practices',
      'Enable VPC flow logs for encryption using conventional setup approaches following the recommended security practices following typical deployment workflows using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'Service mesh provides mTLS (mutual TLS) for service-to-service encryption and authentication: Istio: (1) Deploys Envoy sidecar proxies alongside each pod. (2) Sidecars handle all network traffic. (3) Automatic mTLS: encrypts all traffic between services. (4) Service identity: verifies caller/callee identities using SPIFFE certificates. (5) Observability: distributed tracing, metrics, access logs. AWS App Mesh: (1) AWS-managed control plane. (2) Uses Envoy proxies. (3) Integrates with EKS, ECS, EC2. (4) mTLS support (configurable). (5) Traffic policies, retries, timeouts. Benefits: (1) Zero-trust security: every service call is authenticated and encrypted. (2) No application code changes. (3) Centralized policy management. (4) Traffic shifting for canary deployments. Security groups (B) control access but do not encrypt. HTTPS (C) requires application changes and does not verify service identity. VPC flow logs (D) are for monitoring, not encryption.',
    tags: ['aws', 'eks', 'service-mesh', 'istio', 'app-mesh', 'mtls', 'security'],
  },

  // ─── ECS QUESTIONS ─────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon ECS and what are its two launch types?',
    options: [
      'ECS is a container orchestration service with EC2 (you manage servers) and Fargate (serverless) launch types',
      'ECS only supports the EC2 launch type and requires you to manage all underlying server infrastructure based on standard operational procedures',
      'ECS is a virtual machine service similar to EC2 according to industry standard guidelines as recommended for high availability',
      'ECS and EKS are the same service based on production deployment experience as recommended for high availability following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'Amazon Elastic Container Service (ECS) is a fully managed container orchestration service. Launch types: (1) EC2: you provision and manage EC2 instances that run containers. You control instance types, scaling, and patching. (2) Fargate: serverless — AWS manages the infrastructure. You define CPU/memory per task, and AWS handles the rest. Choose EC2 for: cost optimization with Reserved Instances, GPU workloads, specific instance requirements. Choose Fargate for: simplicity, variable workloads, avoiding server management.',
    tags: ['aws', 'ecs', 'containers', 'fargate', 'ec2'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is an ECS Task Definition and what does it contain?',
    options: [
      'A blueprint specifying container images, CPU/memory, ports, environment variables, and IAM roles',
      'A running instance of a container in the ECS cluster that cannot be modified after creation using established implementation patterns',
      'The EC2 instance configuration for ECS using standard configuration methods as suggested by cloud architects',
      'A networking configuration for VPC which follows AWS recommended architecture patterns following common design principles',
    ],
    correctIndex: 0,
    explanation: 'Task Definition is a JSON template that describes how to run containers. It includes: (1) Container image (ECR URI or Docker Hub). (2) CPU and memory allocation. (3) Port mappings. (4) Environment variables. (5) Task IAM role (for container AWS API access). (6) Execution role (for ECS agent to pull images, send logs). (7) Log configuration (CloudWatch, Splunk). (8) Volumes. (9) Health checks. Task Definitions are versioned — each update creates a new revision.',
    tags: ['aws', 'ecs', 'task-definition', 'containers', 'configuration'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between an ECS Task and an ECS Service?',
    options: [
      'Task is a running container instance; Service maintains desired count of tasks and handles load balancing',
      'Task and Service are the same concept in ECS with no functional differences between them using conventional setup approaches',
      'Service runs containers; Task manages the cluster following the recommended security practices following the recommended security practices',
      'Task is for Fargate; Service is for EC2 as suggested by cloud architects using established implementation patterns and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Task: a running instantiation of a Task Definition — one or more containers running together. Tasks can be run standalone (one-off batch jobs) or as part of a Service. Service: ensures a specified number of tasks are running at all times. If a task fails, the service launches a replacement. Services integrate with load balancers (ALB/NLB) for traffic distribution. Use tasks for: batch jobs, one-off scripts. Use services for: web applications, APIs, long-running processes.',
    tags: ['aws', 'ecs', 'task', 'service', 'containers'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between ECS Task Role and Execution Role?',
    options: [
      'Task Role: permissions for your application code. Execution Role: permissions for ECS agent to pull images and send logs',
      'They are identical and can be used interchangeably for all ECS operations using standard configuration methods following common design principles',
      'Task Role is for Fargate; Execution Role is for EC2 and this approach is commonly used in enterprise settings which follows AWS recommended architecture patterns',
      'Execution Role runs the container; Task Role is deprecated as recommended for high availability as recommended for high availability',
    ],
    correctIndex: 0,
    explanation: 'Two distinct IAM roles: (1) Task Role (taskRoleArn): assumed by your container application. Grants permissions your code needs (S3 access, DynamoDB, SQS). Your app uses AWS SDK with these credentials. (2) Execution Role (executionRoleArn): used by the ECS agent/Fargate. Permissions to: pull images from ECR, send logs to CloudWatch, retrieve secrets from Secrets Manager/Parameter Store. Both are required for most production workloads. Without execution role, ECS cannot pull private ECR images.',
    tags: ['aws', 'ecs', 'iam', 'task-role', 'execution-role', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does ECS integrate with ALB for dynamic port mapping?',
    options: [
      'ECS registers tasks with ALB target group using random host ports; ALB routes to the correct port automatically',
      'You must manually configure each port mapping in the ALB listener rules for every container instance following common design principles',
      'ECS does not support ALB integration following the recommended security practices following common design principles following common design principles',
      'Only NLB supports ECS, not ALB following common design principles implementing typical operational patterns based on standard operational procedures',
    ],
    correctIndex: 0,
    explanation: 'Dynamic port mapping allows running multiple tasks on the same EC2 instance: (1) Task Definition sets containerPort (e.g., 80) but hostPort = 0 (dynamic). (2) ECS assigns random ephemeral host ports (32768-65535) when launching tasks. (3) ECS automatically registers each task with the ALB target group using its instance IP and dynamic port. (4) ALB routes traffic to the correct port. (5) When tasks stop, ECS deregisters them from the target group. This enables high container density per instance. Fargate uses awsvpc mode where each task gets its own IP, eliminating port conflicts.',
    tags: ['aws', 'ecs', 'alb', 'dynamic-port-mapping', 'load-balancing'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your ECS task fails to start with "CannotPullContainerError". How do you debug this?',
    options: [
      'Check: execution role has ECR permissions, VPC has NAT/VPC endpoint for ECR, security group allows HTTPS outbound',
      'This error means the container image does not exist and cannot be debugged further using standard configuration methods as recommended for high availability',
      'Restart the ECS cluster to fix the issue using standard configuration methods and this approach is commonly used in enterprise settings',
      'ECS tasks always pull images successfully using established implementation patterns using conventional setup approaches which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'CannotPullContainerError troubleshooting: (1) Execution Role: must have ecr:GetAuthorizationToken, ecr:BatchGetImage, ecr:GetDownloadUrlForLayer. (2) Network: for private subnets, need NAT Gateway or VPC endpoints for ECR (com.amazonaws.region.ecr.dkr and .ecr.api) and S3 (image layers stored in S3). (3) Security Group: allow HTTPS (443) outbound. (4) Image URI: verify ECR repository name and tag are correct. (5) ECR permissions: repository policy must allow the execution role. Check ECS task stopped reason and CloudWatch logs for details.',
    tags: ['aws', 'ecs', 'ecr', 'troubleshooting', 'pull-error'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your ECS container keeps restarting in a crash loop. How do you investigate?',
    options: [
      'Check CloudWatch logs for application errors, review task stopped reason, verify health check configuration, check resource limits',
      'Crash loops cannot be debugged in ECS and require migration to EKS for proper investigation as documented in official AWS best practices which follows AWS recommended architecture patterns',
      'Delete and recreate the service which follows AWS recommended architecture patterns based on production deployment experience using conventional setup approaches',
      'Increase the task count which follows AWS recommended architecture patterns as recommended for high availability according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'Debug ECS crash loops: (1) CloudWatch Logs: check container logs for application errors, exceptions, stack traces. (2) Task stopped reason: in ECS console, view stopped tasks and check "Stopped reason" (e.g., "Essential container exited", "OutOfMemory"). (3) Health checks: if ALB health check fails, ECS terminates the task. Verify health check path returns 200, increase grace period for slow-starting apps. (4) Resource limits: check if container is being OOM-killed (memory limit too low). (5) Exit code: 137 = OOM killed, 1 = application error, 143 = SIGTERM. (6) Enable ECS Exec for interactive debugging: aws ecs execute-command --task <id> --command "/bin/sh".',
    tags: ['aws', 'ecs', 'troubleshooting', 'crash-loop', 'debugging'],
  },

  // ─── ECR QUESTIONS ─────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon ECR and how do you authenticate Docker to push images?',
    options: [
      'ECR is a managed container registry; authenticate with: aws ecr get-login-password | docker login',
      'ECR uses Docker Hub credentials and does not require separate authentication configuration using conventional setup approaches',
      'ECR does not require authentication using conventional setup approaches as recommended for high availability',
      'ECR only stores Kubernetes configs following typical deployment workflows as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'Amazon Elastic Container Registry (ECR) is a fully managed Docker container registry. Authentication: aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin <account>.dkr.ecr.us-east-1.amazonaws.com. Token is valid for 12 hours. IAM permissions needed: ecr:GetAuthorizationToken, ecr:BatchCheckLayerAvailability, ecr:PutImage, etc. Benefits over Docker Hub: integrated with IAM, VPC endpoints for private access, image scanning, lifecycle policies, encryption at rest.',
    tags: ['aws', 'ecr', 'docker', 'authentication', 'container-registry'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you set up ECR lifecycle policies to automatically clean up old images?',
    options: [
      'Create lifecycle rules that expire images based on age or count, keeping only recent tags',
      'ECR automatically deletes old images without any configuration required from the user which follows AWS recommended architecture patterns',
      'Manual deletion is the only option using established implementation patterns implementing typical operational patterns',
      'Use S3 lifecycle policies on ECR following typical deployment workflows which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'ECR lifecycle policies automate image cleanup. Example policy: (1) Keep only last 10 images with tag prefix "v". (2) Delete untagged images older than 1 day. (3) Delete images older than 30 days. Policy is JSON with rules specifying: tagStatus (tagged/untagged/any), tagPrefixList, countType (imageCountMoreThan/sinceImagePushed), countNumber/countUnit. Apply via console or AWS CLI. This prevents repository bloat and reduces storage costs. Untagged images accumulate when you push with the same tag repeatedly.',
    tags: ['aws', 'ecr', 'lifecycle-policy', 'cleanup', 'cost-optimization'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you scan ECR images for security vulnerabilities?',
    options: [
      'Enable scan on push in repository settings or use ECR Enhanced Scanning with Inspector for deeper analysis',
      'ECR does not support vulnerability scanning and requires third-party tools exclusively and this approach is commonly used in enterprise settings',
      'Scanning happens automatically on all registries as recommended for high availability according to industry standard guidelines',
      'Use docker scan locally only as suggested by cloud architects using conventional setup approaches as suggested by cloud architects',
    ],
    correctIndex: 0,
    explanation: 'ECR offers two scanning options: (1) Basic scanning: free, uses Clair database, scans for OS package vulnerabilities. Enable "Scan on push" per repository. (2) Enhanced scanning (Inspector): paid, continuous scanning, deeper analysis including application dependencies (Java, Python, Node.js packages), integrates with Security Hub. View results in ECR console or via API. Filter by severity (CRITICAL, HIGH, MEDIUM, LOW). Best practice: fail CI/CD pipeline if CRITICAL vulnerabilities found. Use aws ecr describe-image-scan-findings to get results programmatically.',
    tags: ['aws', 'ecr', 'security', 'vulnerability-scanning', 'inspector'],
  },

  // ─── ALB QUESTIONS ─────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is an ALB target group and what can be a target?',
    options: [
      'A target group routes requests to registered targets: EC2 instances, IP addresses, Lambda functions, or other ALBs',
      'Target groups can only contain EC2 instances and no other resource types according to industry standard guidelines as documented in official AWS best practices',
      'Target group is the same as security group following typical deployment workflows following common design principles as documented in official AWS best practices',
      'Target groups are only for NLB which follows AWS recommended architecture patterns which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'Target groups are used by ALB/NLB to route requests. Target types: (1) instance: route to EC2 instance IDs. (2) ip: route to specific IP addresses (useful for containers, on-premises via Direct Connect). (3) lambda: invoke Lambda function (ALB only). (4) alb: route to another ALB (for advanced architectures). Target groups define: health check settings, protocol/port, stickiness, deregistration delay. One ALB can have multiple target groups with path/host-based routing.',
    tags: ['aws', 'alb', 'target-group', 'load-balancing', 'routing'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is path-based routing in ALB?',
    options: [
      'Route requests to different target groups based on URL path (e.g., /api/* to backend, /images/* to S3)',
      'Path-based routing only works with CloudFront and is not supported directly on ALB as suggested by cloud architects',
      'It routes based on source IP and this approach is commonly used in enterprise settings as recommended for high availability',
      'It routes based on HTTP method as recommended for high availability as recommended for high availability following common design principles',
    ],
    correctIndex: 0,
    explanation: 'ALB listener rules support path-based routing: (1) Create multiple target groups (api-servers, image-servers, default). (2) Add listener rules with path conditions: IF path is /api/* THEN forward to api-servers, IF path is /images/* THEN forward to image-servers, default action forwards to default-servers. (3) Rules are evaluated in priority order. Use cases: microservices (each service on different path), separating static/dynamic content, canary deployments with weighted routing.',
    tags: ['aws', 'alb', 'path-based-routing', 'listener-rules', 'microservices'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is connection draining (deregistration delay) in ALB and why is it important?',
    options: [
      'Time ALB waits for in-flight requests to complete before removing a target; prevents request failures during deployments',
      'It immediately terminates all connections when a target is removed from the target group using established implementation patterns',
      'Connection draining is a CloudFront feature based on production deployment experience following typical deployment workflows based on standard operational procedures',
      'It increases connection timeout according to industry standard guidelines as suggested by cloud architects and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Deregistration delay (default 300 seconds): when a target is deregistered (scaling down, deployment), ALB stops sending new requests but waits for existing requests to complete. Important for: (1) Zero-downtime deployments: old tasks finish processing before termination. (2) Graceful shutdown: applications can complete in-progress work. (3) Long-running requests: prevent timeouts for slow operations. Configure per target group. Set lower (30-60s) for fast APIs, higher for long-running operations. ECS/EKS integrate with this for smooth deployments.',
    tags: ['aws', 'alb', 'deregistration-delay', 'connection-draining', 'deployments'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Users are getting 502 Bad Gateway errors from your ALB. How do you troubleshoot?',
    options: [
      'Check: target health (all unhealthy?), security groups allow ALB to reach targets, app is listening on correct port, app returns valid HTTP response',
      '502 errors are always caused by ALB misconfiguration and cannot originate from backend services based on production deployment experience based on standard operational procedures',
      'Increase ALB timeout which follows AWS recommended architecture patterns and this approach is commonly used in enterprise settings and this approach is commonly used in enterprise settings',
      'Switch to NLB following the recommended security practices implementing typical operational patterns which follows AWS recommended architecture patterns and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: '502 Bad Gateway means ALB cannot get a valid response from targets. Troubleshoot: (1) Target health: check target group — are targets healthy? Unhealthy = app not responding to health checks. (2) Security groups: ALB SG must allow outbound to targets; target SG must allow inbound from ALB SG. (3) Port mismatch: ensure app listens on the port target group expects. (4) App crash: check application logs for errors. (5) Response format: app must return valid HTTP response. (6) Timeout: app taking longer than ALB idle timeout (default 60s). Check ALB access logs for target status codes.',
    tags: ['aws', 'alb', '502', 'troubleshooting', 'bad-gateway'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you configure ALB to redirect HTTP to HTTPS and terminate SSL?',
    options: [
      'Create HTTP listener with redirect action to HTTPS, create HTTPS listener with ACM certificate forwarding to target group',
      'SSL termination is not supported on ALB and requires a reverse proxy like Nginx using established implementation patterns as suggested by cloud architects',
      'Configure SSL on each EC2 instance as suggested by cloud architects based on production deployment experience using conventional setup approaches',
      'Use CloudFront for all HTTPS following typical deployment workflows implementing typical operational patterns following common design principles',
    ],
    correctIndex: 0,
    explanation: 'ALB SSL termination setup: (1) Request ACM certificate for your domain (free, auto-renews). (2) Create HTTPS listener (443) on ALB, attach ACM certificate, forward to target group. (3) Create HTTP listener (80) with action: redirect to HTTPS (301 permanent redirect). (4) Target group can use HTTP (port 80) — traffic between ALB and targets is unencrypted but within VPC. (5) For end-to-end encryption: target group uses HTTPS, app has its own certificate. Benefits: offload SSL processing from app servers, centralized certificate management, automatic certificate renewal.',
    tags: ['aws', 'alb', 'ssl', 'https', 'acm', 'redirect'],
  },

  // ─── API GATEWAY QUESTIONS ─────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is AWS API Gateway and what are the three API types it offers?',
    options: [
      'Managed API service offering REST APIs (full features), HTTP APIs (low cost, simpler), and WebSocket APIs (real-time)',
      'API Gateway only supports REST APIs and requires Lambda for WebSocket functionality following the recommended security practices',
      'API Gateway is a load balancer based on production deployment experience as recommended for high availability following typical deployment workflows',
      'API Gateway and ALB are identical according to industry standard guidelines using established implementation patterns based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'API Gateway types: (1) REST API: full-featured, supports request validation, caching, WAF integration, usage plans, API keys, resource policies. Higher cost. (2) HTTP API: simpler, 70% cheaper, supports OIDC/OAuth2 natively, faster, good for most use cases. Lacks some REST API features (caching, WAF, resource policies). (3) WebSocket API: bidirectional real-time communication (chat, live updates, gaming). Maintains persistent connections. All integrate with Lambda, HTTP backends, and other AWS services.',
    tags: ['aws', 'api-gateway', 'rest-api', 'http-api', 'websocket'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is a stage in API Gateway?',
    options: [
      'A named deployment snapshot like dev, staging, prod with its own URL and configuration',
      'Stages are only used for versioning Lambda functions and are not part of API Gateway using standard configuration methods',
      'A stage is equivalent to an API method based on standard operational procedures using conventional setup approaches',
      'Stages are deprecated in favor of aliases as documented in official AWS best practices which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'Stages represent deployment environments. Each stage: (1) Has unique invoke URL: https://api-id.execute-api.region.amazonaws.com/stage-name. (2) Can have different settings: throttling, logging, caching. (3) Stage variables: key-value pairs for environment-specific config (e.g., LAMBDA_ALIAS=prod). (4) Can point to different Lambda aliases or backends. Common pattern: dev stage → $LATEST Lambda, prod stage → prod Lambda alias. Canary deployments: route percentage of traffic to canary stage for testing.',
    tags: ['aws', 'api-gateway', 'stages', 'deployment', 'environments'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is a Lambda authorizer in API Gateway?',
    options: [
      'A Lambda function that validates tokens/credentials and returns an IAM policy allowing or denying the request',
      'Lambda authorizer is the IAM role Lambda uses to execute and has no relation to API Gateway using conventional setup approaches',
      'A built-in JWT validator following common design principles as suggested by cloud architects as documented in official AWS best practices',
      'A replacement for Cognito as recommended for high availability following common design principles as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'Lambda authorizer (custom authorizer) runs before your API backend. Types: (1) Token-based: receives authorization token (JWT, custom), validates it, returns IAM policy. (2) Request-based: receives all request parameters (headers, query strings), flexible validation. Use cases: custom token formats, integration with external IdPs, complex authorization logic. Returns: principal ID, IAM policy (Allow/Deny), context (passed to backend). Caching: cache authorization results by token to reduce Lambda invocations. Alternative: Cognito authorizer for standard JWT validation.',
    tags: ['aws', 'api-gateway', 'lambda-authorizer', 'authentication', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your frontend gets CORS errors when calling your API Gateway endpoint. How do you fix it?',
    options: [
      'Enable CORS on the API Gateway resource, configure allowed origins, methods, and headers in the response',
      'CORS is only a browser issue and does not require any server-side configuration changes using established implementation patterns',
      'Disable CORS in the browser implementing typical operational patterns and this approach is commonly used in enterprise settings',
      'Use POST instead of GET as suggested by cloud architects using standard configuration methods based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'CORS must be configured on API Gateway: (1) For REST APIs: enable CORS on each resource, configure Access-Control-Allow-Origin, Access-Control-Allow-Methods, Access-Control-Allow-Headers. API Gateway handles OPTIONS preflight requests. (2) For HTTP APIs: configure CORS in API settings with allowed origins, methods, headers, max age. (3) For Lambda proxy integration: your Lambda must also return CORS headers in its response. Common mistake: enabling CORS on API Gateway but Lambda not returning headers. Use * for origin in dev, specific domain in prod for security.',
    tags: ['aws', 'api-gateway', 'cors', 'troubleshooting', 'frontend'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'How do you implement API versioning in API Gateway without breaking existing clients?',
    options: [
      'Use path-based versioning (/v1/users, /v2/users), header-based versioning, or deploy to separate stages',
      'API versioning is not supported and requires creating completely new API Gateway instances as suggested by cloud architects',
      'Clients must always use the latest version following the recommended security practices as suggested by cloud architects',
      'Use query parameters only according to industry standard guidelines using standard configuration methods following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'API versioning strategies: (1) Path versioning: /v1/resource, /v2/resource — clearest, most common. Create separate resources and methods per version. (2) Header versioning: Accept: application/vnd.api.v1+json. Use Lambda to route based on header. (3) Query parameter: ?version=1. Less RESTful but works. (4) Stage-based: v1 stage, v2 stage with different URLs. (5) Canary deployments: gradually shift traffic to new version. Best practice: maintain backward compatibility, deprecate old versions with timeline, version from day one.',
    tags: ['aws', 'api-gateway', 'versioning', 'api-design', 'backward-compatibility'],
  },

  // ─── AUTO SCALING QUESTIONS ─────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between Launch Template and Launch Configuration in Auto Scaling?',
    options: [
      'Launch Template is newer, versioned, supports mixed instances; Launch Configuration is legacy and immutable',
      'They are identical and can be used interchangeably in all situations implementing typical operational patterns using established implementation patterns',
      'Launch Configuration supports more features implementing typical operational patterns according to industry standard guidelines',
      'Launch Template is only for Spot instances following common design principles as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'Launch Template (recommended): (1) Supports versioning — update template, create new version, ASG can use latest or specific version. (2) Mixed instance types and purchase options (On-Demand + Spot). (3) Supports T2/T3 unlimited. (4) Can be used for standalone EC2 launches too. Launch Configuration (legacy): (1) Immutable — must create new LC for any change. (2) Single instance type. (3) Being phased out. Always use Launch Templates for new ASGs.',
    tags: ['aws', 'auto-scaling', 'launch-template', 'launch-configuration', 'ec2'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are the types of Auto Scaling policies and when do you use each?',
    options: [
      'Target tracking (maintain metric at target), step scaling (tiered responses), simple scaling (single adjustment), scheduled scaling (time-based)',
      'Auto Scaling only supports simple scaling with a single threshold configuration following typical deployment workflows as recommended for high availability',
      'All scaling must be manual as suggested by cloud architects implementing typical operational patterns following typical deployment workflows as suggested by cloud architects',
      'Scaling policies are the same as launch templates as recommended for high availability based on production deployment experience as suggested by cloud architects',
    ],
    correctIndex: 0,
    explanation: 'Scaling policy types: (1) Target tracking: maintain metric at target (e.g., CPU at 50%). ASG automatically adjusts. Simplest, recommended for most cases. (2) Step scaling: different adjustments for different alarm thresholds (CPU 50-70% add 1, 70-90% add 2, >90% add 4). (3) Simple scaling: single adjustment when alarm triggers, then wait for cooldown. Legacy, less flexible. (4) Scheduled scaling: scale based on schedule (increase capacity at 9am, decrease at 6pm). Good for predictable patterns. (5) Predictive scaling: ML-based forecasting. Combine multiple policies for best results.',
    tags: ['aws', 'auto-scaling', 'scaling-policies', 'target-tracking', 'step-scaling'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is a lifecycle hook in Auto Scaling and when would you use it?',
    options: [
      'Pauses instance launch/termination to run custom actions like configuration or data backup',
      'Lifecycle hooks control the entire lifecycle of the Auto Scaling group itself which follows AWS recommended architecture patterns',
      'Hooks for monitoring ASG following common design principles using conventional setup approaches as documented in official AWS best practices',
      'A type of health check using standard configuration methods as recommended for high availability and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Lifecycle hooks pause ASG during instance launch or termination: (1) Launching hook: instance starts in Pending:Wait state. Run custom actions: install software, register with monitoring, pull configuration. Complete with aws autoscaling complete-lifecycle-action. (2) Terminating hook: instance enters Terminating:Wait. Backup data, deregister from external services, drain connections. Timeout: default 1 hour, configurable. Integration: EventBridge triggers Lambda or SNS when hooks fire. Use cases: complex bootstrapping, graceful shutdown, compliance logging.',
    tags: ['aws', 'auto-scaling', 'lifecycle-hooks', 'launch', 'termination'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your ASG keeps terminating and launching instances in a loop. What is causing this?',
    options: [
      'Health check failing: app not ready before health check, wrong health check path, or ELB health check mismatch with app startup time',
      'This is normal Auto Scaling behavior and indicates the system is working correctly following common design principles as documented in official AWS best practices',
      'The ASG desired capacity is set to zero following the recommended security practices following common design principles based on standard operational procedures',
      'You have too many Availability Zones configured and this approach is commonly used in enterprise settings based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Instance churn (launch → fail health check → terminate → launch) causes: (1) Health check grace period too short: app needs 2 minutes to start, health check starts at 30 seconds. Solution: increase grace period. (2) Wrong health check: ELB health check path returns 404, but app is fine. (3) App crash: check CloudWatch logs, instance is launching but app fails. (4) Resource exhaustion: app starts but OOMs. (5) Dependency failure: app cannot connect to database on startup. Debug: check ASG Activity History, EC2 instance logs, ELB target health. Set health check grace period to cover full app startup time.',
    tags: ['aws', 'auto-scaling', 'instance-churn', 'health-check', 'troubleshooting'],
  },

  // ─── ROUTE 53 QUESTIONS ─────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is an Alias record in Route 53 and how does it differ from CNAME?',
    options: [
      'Alias is AWS-specific, free, works at zone apex, resolves to AWS resource IPs; CNAME is standard DNS, costs per query, cannot be at apex',
      'Alias and CNAME are identical in functionality and can be used interchangeably following the recommended security practices implementing typical operational patterns',
      'CNAME is faster than Alias and this approach is commonly used in enterprise settings according to industry standard guidelines and this approach is commonly used in enterprise settings',
      'Alias only works with S3 using conventional setup approaches based on production deployment experience using standard configuration methods as recommended for high availability',
    ],
    correctIndex: 0,
    explanation: 'Alias records (Route 53 specific): (1) Work at zone apex (example.com, not just www.example.com). (2) No charge for queries to AWS resources. (3) Automatically resolve to current IPs of AWS resources (ALB, CloudFront, S3). (4) Support health checks. CNAME records: (1) Standard DNS. (2) Cannot be at zone apex. (3) Charged per query. (4) Points to another domain name. Use Alias for: ALB, CloudFront, S3 website, API Gateway, Elastic Beanstalk. Use CNAME for: non-AWS resources, third-party services.',
    tags: ['aws', 'route53', 'alias', 'cname', 'dns'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What routing policies does Route 53 support?',
    options: [
      'Simple, weighted, latency-based, failover, geolocation, geoproximity, multivalue answer',
      'Route 53 only supports simple round-robin routing and nothing else and this approach is commonly used in enterprise settings',
      'Routing policies are managed by ALB as suggested by cloud architects which follows AWS recommended architecture patterns',
      'Only failover is supported implementing typical operational patterns according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'Route 53 routing policies: (1) Simple: single resource, returns all values randomly. (2) Weighted: distribute traffic by weight (70/30 split). (3) Latency: route to lowest latency region for user. (4) Failover: primary/secondary with health checks. (5) Geolocation: route based on user location (US users → US servers). (6) Geoproximity: route based on geographic location with bias adjustment. (7) Multivalue: return multiple healthy IPs, client chooses. Use cases: blue-green deployments (weighted), disaster recovery (failover), global distribution (latency/geolocation).',
    tags: ['aws', 'route53', 'routing-policies', 'dns', 'traffic-management'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'You updated a DNS record in Route 53 but users are still hitting the old IP. Why?',
    options: [
      'DNS TTL caching: clients and resolvers cache the old record until TTL expires',
      'Route 53 changes are instant and this indicates a different problem implementing typical operational patterns',
      'Route 53 requires a reboot as suggested by cloud architects which follows AWS recommended architecture patterns',
      'The hosted zone is misconfigured using conventional setup approaches and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'DNS propagation delay due to TTL (Time To Live): (1) When you change a record, resolvers and clients may have cached the old value. (2) They will use the cached value until TTL expires. (3) If TTL was 24 hours, full propagation takes up to 24 hours. Solutions: (1) Lower TTL before planned changes (e.g., to 60 seconds). (2) Wait for old TTL to expire. (3) Some ISP resolvers ignore low TTLs. Best practice: for frequently changed records (blue-green), keep TTL low (60-300 seconds). For stable records, higher TTL (3600+) reduces DNS queries.',
    tags: ['aws', 'route53', 'dns', 'ttl', 'propagation'],
  },

  // ─── EVENTBRIDGE QUESTIONS ─────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon EventBridge and how does it differ from SNS?',
    options: [
      'EventBridge is an event bus with content-based filtering and schema registry; SNS is pub/sub with topic-based routing',
      'EventBridge and SNS are identical services with different names for historical reasons which follows AWS recommended architecture patterns',
      'SNS is for events; EventBridge is for notifications using standard configuration methods according to industry standard guidelines',
      'EventBridge is deprecated in favor of SNS as suggested by cloud architects following typical deployment workflows which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'EventBridge vs SNS: EventBridge: (1) Content-based filtering (filter by event fields). (2) Schema registry for event discovery. (3) Built-in support for AWS service events (EC2 state change, etc.). (4) Partner integrations (Zendesk, Datadog). (5) Better for event-driven architectures. SNS: (1) Topic-based routing. (2) Simpler pub/sub. (3) Supports SMS, email, HTTP endpoints. (4) Higher throughput for simple fanout. Use EventBridge for: complex routing, AWS event integration, SaaS events. Use SNS for: simple fanout, mobile push, email notifications.',
    tags: ['aws', 'eventbridge', 'sns', 'events', 'event-driven'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'How do you trigger a Lambda function when an EC2 instance changes state using EventBridge?',
    options: [
      'Create EventBridge rule with event pattern matching EC2 state-change events, set Lambda as target',
      'EC2 does not send events to EventBridge and requires CloudWatch alarms exclusively and this approach is commonly used in enterprise settings',
      'Use SNS only based on standard operational procedures and this approach is commonly used in enterprise settings',
      'Poll EC2 API from Lambda using established implementation patterns following common design principles and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EventBridge rule for EC2 state changes: (1) Source: aws.ec2. (2) Detail-type: EC2 Instance State-change Notification. (3) Detail: state: ["stopping", "stopped", "terminated"]. (4) Target: Lambda function. Example pattern: {"source": ["aws.ec2"], "detail-type": ["EC2 Instance State-change Notification"], "detail": {"state": ["stopped"]}}. Use cases: auto-remediation, inventory updates, notification on termination, compliance logging. EventBridge receives events from 100+ AWS services automatically.',
    tags: ['aws', 'eventbridge', 'ec2', 'lambda', 'automation'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you create a scheduled cron job using EventBridge?',
    options: [
      'Create a rule with schedule expression (cron or rate), set Lambda or other service as target',
      'EventBridge does not support scheduling and requires CloudWatch Events instead following common design principles',
      'Use EC2 cron only based on standard operational procedures according to industry standard guidelines using conventional setup approaches',
      'Schedule is only for Step Functions following the recommended security practices using established implementation patterns',
    ],
    correctIndex: 0,
    explanation: 'EventBridge Scheduler (or scheduled rules): (1) Rate expression: rate(5 minutes), rate(1 hour), rate(1 day). (2) Cron expression: cron(0 12 * * ? *) = every day at 12:00 UTC. (3) Target: Lambda, Step Functions, ECS task, SNS, SQS, etc. Use cases: daily reports, cleanup jobs, health checks, data sync. EventBridge Scheduler (newer): more features like time zones, flexible time windows, one-time schedules. Note: cron uses UTC by default.',
    tags: ['aws', 'eventbridge', 'scheduler', 'cron', 'automation'],
  },

  // ─── EBS QUESTIONS ─────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What EBS volume types exist and when do you use each?',
    options: [
      'gp3/gp2 (general SSD), io2/io1 (high IOPS), st1 (throughput HDD), sc1 (cold HDD)',
      'EBS only offers one volume type that automatically adjusts performance as documented in official AWS best practices',
      'SSD types are deprecated based on production deployment experience using established implementation patterns',
      'HDD is faster than SSD based on production deployment experience using established implementation patterns',
    ],
    correctIndex: 0,
    explanation: 'EBS volume types: (1) gp3: general purpose SSD, 3000 IOPS baseline, independently provision IOPS/throughput. Best default choice. (2) gp2: older general SSD, IOPS scales with size (3 IOPS/GB). (3) io2/io1: provisioned IOPS SSD, up to 64,000 IOPS, for databases requiring consistent high performance. (4) st1: throughput-optimized HDD, for big data, log processing (500 MB/s). (5) sc1: cold HDD, lowest cost, for infrequently accessed data. Use gp3 for: most workloads. Use io2 for: critical databases. Use st1/sc1 for: large sequential workloads, archives.',
    tags: ['aws', 'ebs', 'volume-types', 'storage', 'performance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between gp2 and gp3 EBS volumes?',
    options: [
      'gp3 has independent IOPS/throughput provisioning and baseline 3000 IOPS; gp2 scales IOPS with volume size',
      'gp2 and gp3 are identical in performance and pricing with no functional differences following typical deployment workflows',
      'gp3 is slower than gp2 based on production deployment experience using established implementation patterns as recommended for high availability',
      'gp2 supports more IOPS based on standard operational procedures following typical deployment workflows using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'gp2 vs gp3: gp2: (1) IOPS scales with size: 3 IOPS per GB, minimum 100, max 16,000. (2) Small volumes get burst credits. (3) Throughput linked to IOPS. gp3: (1) Baseline 3,000 IOPS and 125 MB/s included for all sizes. (2) Independently provision up to 16,000 IOPS and 1,000 MB/s. (3) 20% cheaper than gp2 at baseline. Always use gp3 for new volumes — better performance, lower cost, more control. Migrate existing gp2 to gp3 with no downtime (modify volume type).',
    tags: ['aws', 'ebs', 'gp2', 'gp3', 'performance', 'cost'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Can you change EBS volume type or size without downtime?',
    options: [
      'Yes, use Elastic Volumes to modify type/size/IOPS while volume is in use',
      'Changing volume type requires detaching the volume and stopping the instance',
      'Volume modifications are not supported',
      'Only size can be changed',
    ],
    correctIndex: 0,
    explanation: 'EBS Elastic Volumes allow live modifications: (1) Change volume type (gp2 → gp3, gp3 → io2). (2) Increase size (cannot decrease). (3) Adjust IOPS and throughput (gp3, io1/io2). Process: aws ec2 modify-volume --volume-id vol-xxx --volume-type gp3. Volume enters "optimizing" state. Performance may be slightly reduced during optimization. Limitations: wait 6 hours between modifications, cannot decrease size. For size increase: extend filesystem after volume modification (resize2fs, xfs_growfs).',
    tags: ['aws', 'ebs', 'elastic-volumes', 'modification', 'downtime'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your database on EC2 has high EBS latency. Which volume type should you switch to?',
    options: [
      'io2 Block Express for consistent, high IOPS with sub-millisecond latency',
      'st1 for better throughput characteristics that improve database latency as suggested by cloud architects',
      'sc1 for cost savings as documented in official AWS best practices following common design principles',
      'gp2 for burst capability based on production deployment experience following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'For latency-sensitive databases: io2/io2 Block Express: (1) Provisioned IOPS: up to 64,000 IOPS (io2 Block Express: 256,000). (2) Sub-millisecond latency. (3) 99.999% durability (vs 99.8-99.9% for other types). (4) Multi-attach for shared storage. Use for: production databases (Oracle, SQL Server, MySQL), latency-critical applications. Cost: higher than gp3 but worth it for critical workloads. Also consider: instance type (some have EBS-optimized networking), RAID 0 for striping, RDS/Aurora for managed databases.',
    tags: ['aws', 'ebs', 'io2', 'database', 'latency', 'performance'],
  },

  // ─── EFS QUESTIONS ─────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon EFS and how does it differ from EBS?',
    options: [
      'EFS is shared network filesystem accessible by multiple instances; EBS is block storage attached to single instance',
      'EFS and EBS are the same service with different names and can be used interchangeably using standard configuration methods following typical deployment workflows',
      'EBS can be shared across multiple instances as documented in official AWS best practices following typical deployment workflows',
      'EFS is faster than EBS as suggested by cloud architects implementing typical operational patterns and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EFS vs EBS: EFS (Elastic File System): (1) NFS-based shared filesystem. (2) Multiple EC2 instances can mount simultaneously. (3) Automatically scales to petabytes. (4) Regional — accessible across AZs. (5) Higher latency than EBS. (6) Pay per GB stored. Use for: shared content, web server files, home directories. EBS: (1) Block storage attached to single instance (except Multi-Attach io1/io2). (2) Fixed size, manually provision. (3) Single AZ. (4) Lower latency. Use for: databases, boot volumes, single-instance storage.',
    tags: ['aws', 'efs', 'ebs', 'storage', 'shared-filesystem'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What throughput modes does EFS support?',
    options: [
      'Bursting (scales with size), Provisioned (fixed throughput), Elastic (auto-scales based on workload)',
      'EFS has a single throughput mode that cannot be configured by the user according to industry standard guidelines',
      'EFS does not have throughput modes based on standard operational procedures following typical deployment workflows',
      'Throughput is unlimited implementing typical operational patterns using established implementation patterns as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'EFS throughput modes: (1) Bursting: throughput scales with filesystem size (50 MB/s per TB, burst to 100 MB/s). Good for: light, spiky workloads. (2) Provisioned: specify fixed throughput (up to 1+ GB/s) regardless of size. Good for: consistent high throughput needs. (3) Elastic (recommended): automatically scales throughput based on workload. Pay for what you use. Good for: unpredictable workloads, simplest option. Switch modes anytime. Elastic mode is newest and recommended for most use cases.',
    tags: ['aws', 'efs', 'throughput', 'performance', 'bursting'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Multiple EC2 instances need access to the same files. Should you use EBS or EFS?',
    options: [
      'EFS — it supports concurrent access from multiple instances across AZs',
      'EBS with Multi-Attach works for all use cases involving multiple instances',
      'Create copies of EBS for each instance',
      'Use S3 instead',
    ],
    correctIndex: 0,
    explanation: 'Use EFS for shared filesystem: (1) NFS protocol, standard POSIX permissions. (2) Mount on hundreds of instances simultaneously. (3) Cross-AZ access within region. (4) Automatic scaling. EBS Multi-Attach: (1) Only io1/io2 volumes. (2) Same AZ only. (3) Requires cluster-aware filesystem (not standard ext4/xfs). (4) Limited to 16 instances. For most shared storage needs, EFS is the right choice. Alternatives: FSx for Lustre (HPC), FSx for Windows (SMB), S3 (object storage with different access pattern).',
    tags: ['aws', 'efs', 'ebs', 'shared-storage', 'multi-instance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you mount EFS in a Fargate container?',
    options: [
      'Configure EFS volume in task definition with file system ID and mount point, ensure security group allows NFS',
      'EFS cannot be mounted in Fargate containers and requires EC2 launch type exclusively based on standard operational procedures',
      'Use S3 instead as recommended for high availability as suggested by cloud architects following common design principles',
      'Mount via SSH which follows AWS recommended architecture patterns and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EFS with Fargate: (1) Task definition: add volume with efsVolumeConfiguration (fileSystemId, rootDirectory, transitEncryption). (2) Container definition: mount the volume to a path. (3) Security: EFS security group must allow NFS (2049) from Fargate tasks. (4) IAM: task role needs elasticfilesystem:ClientMount permission. (5) Access points: optionally use EFS access points for POSIX user/group enforcement. Use cases: shared data between containers, persistent storage for stateful apps, content management. Fargate fully supports EFS since 2020.',
    tags: ['aws', 'efs', 'fargate', 'ecs', 'containers', 'persistent-storage'],
  },

  // ─── FARGATE QUESTIONS ─────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is AWS Fargate and how does it differ from ECS on EC2?',
    options: [
      'Fargate is serverless containers — no EC2 management; you pay per vCPU/memory per second',
      'Fargate and ECS on EC2 are identical with the same pricing and management requirements as documented in official AWS best practices',
      'Fargate is more expensive in all scenarios as recommended for high availability using standard configuration methods',
      'Fargate does not support ECS following the recommended security practices according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'Fargate is serverless compute for containers: (1) No EC2 instances to manage, patch, or scale. (2) Define CPU/memory per task, AWS provisions infrastructure. (3) Pay per vCPU-hour and GB-hour used. (4) Works with both ECS and EKS. ECS on EC2: (1) You manage EC2 instances. (2) More control, can use Reserved Instances for cost savings. (3) Required for GPU workloads, Windows containers (until recently). Choose Fargate for: simplicity, variable workloads, small teams. Choose EC2 for: cost optimization with RIs, GPU, specific instance needs.',
    tags: ['aws', 'fargate', 'ecs', 'serverless', 'containers'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does networking work in Fargate with awsvpc mode?',
    options: [
      'Each task gets its own ENI with private IP, directly attached to your VPC subnet',
      'Fargate uses the same networking as EC2 with NAT and requires manual port mapping',
      'Fargate does not support VPC',
      'All tasks share one IP',
    ],
    correctIndex: 0,
    explanation: 'Fargate uses awsvpc network mode: (1) Each task gets dedicated ENI (Elastic Network Interface). (2) Task has its own private IP in your VPC subnet. (3) Security groups apply directly to tasks. (4) No port mapping needed — use actual container ports. (5) Tasks can communicate via private IPs. Benefits: security group per task, VPC Flow Logs per task, simpler networking. Consideration: ENI limits per subnet — ensure enough IPs. For internet access in private subnet, need NAT Gateway or VPC endpoints.',
    tags: ['aws', 'fargate', 'networking', 'awsvpc', 'eni'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Fargate task cannot connect to RDS in the same VPC. What do you check?',
    options: [
      'Security groups: RDS SG must allow inbound from Fargate task SG on database port',
      'Fargate cannot connect to RDS and requires a proxy service using standard configuration methods',
      'RDS must be public as suggested by cloud architects following the recommended security practices',
      'Use Lambda instead and this approach is commonly used in enterprise settings according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'Fargate to RDS connectivity checklist: (1) Security groups: RDS SG allows inbound on DB port (3306/5432) from Fargate task SG. (2) Subnet: Fargate task and RDS in same VPC, route table allows communication. (3) Credentials: task has correct DB host, username, password. (4) IAM: if using IAM authentication, task role has rds-db:connect permission. (5) DNS: VPC DNS resolution enabled. Debug: enable ECS Exec, shell into container, test connection with nc -zv rds-host 5432. Common mistake: security group references wrong source.',
    tags: ['aws', 'fargate', 'rds', 'connectivity', 'troubleshooting'],
  },

  // ─── ECS QUESTIONS ────────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon ECS and what are its main components?',
    options: [
      'Container orchestration service with clusters, services, tasks, and task definitions',
      'ECS is a virtual machine service similar to EC2 but optimized for containers with automatic VM provisioning',
      'ECS is only for running Docker on Lambda',
      'ECS requires Kubernetes knowledge',
    ],
    correctIndex: 0,
    explanation: 'Amazon ECS (Elastic Container Service) is a container orchestration service. Components: (1) Cluster: logical grouping of tasks/services. (2) Task Definition: blueprint specifying container image, CPU, memory, ports, environment variables. (3) Task: running instance of a task definition (one or more containers). (4) Service: maintains desired count of tasks, integrates with load balancer. (5) Container Agent: runs on EC2 instances to communicate with ECS control plane. ECS supports EC2 and Fargate launch types.',
    tags: ['aws', 'ecs', 'containers', 'fundamentals'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between an ECS Task and an ECS Service?',
    options: [
      'Task is a running container instance; Service maintains desired task count and handles scaling/load balancing',
      'Task and Service are the same concept in ECS terminology as suggested by cloud architects following common design principles',
      'Service runs containers; Task defines the cluster infrastructure configuration using conventional setup approaches using standard configuration methods',
      'Task is for Fargate only; Service is for EC2 launch type only as documented in official AWS best practices based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Task: a running instantiation of a task definition — represents one or more containers running together. Tasks can be run standalone (batch jobs) or as part of a service. Service: ensures a specified number of tasks are running and healthy. Services provide: (1) desired count maintenance (replaces failed tasks), (2) load balancer integration, (3) deployment strategies (rolling, blue-green), (4) auto scaling. Use tasks for batch/one-off jobs; use services for long-running applications.',
    tags: ['aws', 'ecs', 'task', 'service', 'fundamentals'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between ECS task IAM role and execution IAM role?',
    options: [
      'Task role: permissions for application code. Execution role: permissions for ECS agent (pull images, push logs)',
      'They are identical and can be used interchangeably in ECS configurations and this approach is commonly used in enterprise settings',
      'Task role is for Fargate; execution role is for EC2 following typical deployment workflows following common design principles',
      'Execution role runs the container; task role defines networking using standard configuration methods using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'Two distinct IAM roles in ECS: (1) Task Role: permissions your application code uses at runtime (S3 access, DynamoDB, SQS). Accessed via container credential provider. (2) Execution Role: permissions the ECS agent needs to launch the task — pull images from ECR, send logs to CloudWatch, retrieve secrets from Secrets Manager/Parameter Store. Both are specified in task definition. Execution role is required for Fargate and ECR private images.',
    tags: ['aws', 'ecs', 'iam', 'task-role', 'execution-role'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does ECS integrate with ALB for dynamic port mapping?',
    options: [
      'Set host port to 0 in task definition; ALB target group uses instance targets; ECS registers dynamic ports automatically',
      'Dynamic port mapping requires manual configuration of each port in ALB listener rules based on standard operational procedures using conventional setup approaches',
      'ALB does not support ECS integration using conventional setup approaches and this approach is commonly used in enterprise settings',
      'Only NLB supports ECS according to industry standard guidelines using established implementation patterns based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Dynamic port mapping allows multiple tasks on the same EC2 instance: (1) Task definition: set hostPort to 0 (or omit it), containerPort to application port (e.g., 8080). (2) ECS assigns random ephemeral host port at runtime. (3) ALB target group: use "instance" target type. (4) ECS service: registers/deregisters tasks with target group automatically, including the dynamic port. (5) ALB routes traffic to correct host:port combinations. This enables high container density per EC2 instance.',
    tags: ['aws', 'ecs', 'alb', 'dynamic-port-mapping', 'load-balancing'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your ECS task keeps failing to start with "CannotPullContainerError". How do you debug?',
    options: [
      'Check: ECR permissions in execution role, VPC endpoints or NAT for ECR access, image URI correctness',
      'This error means the container image is corrupted and must be rebuilt from scratch according to industry standard guidelines',
      'ECS does not support pulling images from ECR based on production deployment experience following common design principles',
      'Restart the ECS cluster as recommended for high availability following typical deployment workflows following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'CannotPullContainerError troubleshooting: (1) Execution role: must have ecr:GetAuthorizationToken, ecr:BatchGetImage, ecr:GetDownloadUrlForLayer. (2) Networking: if in private subnet, needs NAT Gateway or VPC endpoints (ecr.api, ecr.dkr, s3) to reach ECR. (3) Image URI: verify repository URI, tag exists, and region is correct. (4) Repository policy: ECR repository must allow the execution role. (5) Check CloudWatch Logs for detailed error. (6) For Docker Hub: check rate limits (anonymous pulls are limited).',
    tags: ['aws', 'ecs', 'ecr', 'troubleshooting', 'container-pull'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'How do you achieve zero-downtime deployments in ECS?',
    options: [
      'Use rolling update with minimum healthy percent, proper health checks, and deregistration delay',
      'Zero-downtime is not possible with ECS and requires Kubernetes based on production deployment experience using standard configuration methods',
      'Stop all old tasks before starting new ones following the recommended security practices using conventional setup approaches',
      'Use only one task at a time and this approach is commonly used in enterprise settings according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'Zero-downtime ECS deployment: (1) Rolling update deployment type with minimumHealthyPercent=100 and maximumPercent=200 (keeps old tasks running while new ones start). (2) Health checks: ALB health check confirms new tasks are healthy before routing traffic. (3) Deregistration delay: connection draining time for graceful shutdown (default 300s). (4) Container health check: ECS monitors container health via HEALTHCHECK in Dockerfile or healthCheck in task definition. (5) Blue-green deployment via CodeDeploy for instant traffic switch. (6) Grace period: startPeriod allows container warm-up before health checks fail.',
    tags: ['aws', 'ecs', 'zero-downtime', 'deployment', 'rolling-update'],
  },

  // ─── ECR QUESTIONS ────────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon ECR and how do you authenticate Docker to push images?',
    options: [
      'ECR is a managed Docker registry; authenticate with: aws ecr get-login-password | docker login',
      'ECR uses Docker Hub credentials so no additional authentication is needed using conventional setup approaches',
      'ECR does not require authentication based on standard operational procedures using standard configuration methods',
      'ECR only works with EKS and this approach is commonly used in enterprise settings as suggested by cloud architects',
    ],
    correctIndex: 0,
    explanation: 'Amazon ECR (Elastic Container Registry) is a managed Docker container registry. Authentication: (1) Get auth token: aws ecr get-login-password --region us-east-1. (2) Docker login: pipe token to docker login --username AWS --password-stdin <account>.dkr.ecr.<region>.amazonaws.com. (3) Combined: aws ecr get-login-password | docker login --username AWS --password-stdin <uri>. Token valid for 12 hours. IAM user/role needs ecr:GetAuthorizationToken permission. For CI/CD, use credential helpers or service roles.',
    tags: ['aws', 'ecr', 'docker', 'authentication', 'registry'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you set up ECR lifecycle policies to automatically clean up old images?',
    options: [
      'Create lifecycle policy rules that expire images based on count or age, keeping only recent images',
      'ECR automatically deletes all images older than 30 days without any configuration as documented in official AWS best practices',
      'Manual deletion is the only option based on standard operational procedures and this approach is commonly used in enterprise settings',
      'Use S3 lifecycle policies following the recommended security practices according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'ECR lifecycle policies automate image cleanup: (1) Count-based: keep only the N most recent images. (2) Age-based: expire images older than N days. (3) Tag-based: target untagged images or specific tag prefixes. Example policy: keep 10 most recent tagged images, expire untagged images older than 1 day. Benefits: reduce storage costs, maintain security (remove old vulnerable images). Configure via Console, CLI, or Terraform. Policy evaluation is automatic and daily.',
    tags: ['aws', 'ecr', 'lifecycle-policy', 'cleanup', 'cost-optimization'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you scan Docker images for vulnerabilities in ECR?',
    options: [
      'Enable scan on push in repository settings or use ECR Enhanced Scanning with Amazon Inspector',
      'ECR does not support vulnerability scanning and requires third-party tools using standard configuration methods',
      'Scanning is automatic and cannot be configured implementing typical operational patterns using established implementation patterns',
      'Only EKS images can be scanned as suggested by cloud architects implementing typical operational patterns',
    ],
    correctIndex: 0,
    explanation: 'ECR vulnerability scanning options: (1) Basic scanning: uses Clair, scans for CVEs in OS packages. Enable "scan on push" per repository. (2) Enhanced scanning (Inspector): uses Amazon Inspector, deeper scanning including application dependencies (npm, pip packages). Continuous monitoring for new vulnerabilities. View results in ECR console, Inspector, or via API. Integrate with EventBridge for alerts on critical findings. Best practice: enable scan on push for all repositories, block deployments with critical vulnerabilities via CI/CD checks.',
    tags: ['aws', 'ecr', 'security', 'vulnerability-scanning', 'inspector'],
  },

  // ─── ALB QUESTIONS ────────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is an ALB target group and what can be a target?',
    options: [
      'Target group routes requests to registered targets: EC2 instances, IP addresses, Lambda functions, or other ALBs',
      'Target group is another name for security group that controls ALB traffic following the recommended security practices as suggested by cloud architects',
      'Only EC2 instances can be targets as recommended for high availability using established implementation patterns using conventional setup approaches',
      'Target group is the ALB itself following the recommended security practices based on production deployment experience according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'ALB target groups define where to route traffic. Target types: (1) Instance: route to EC2 instance ID (uses dynamic port). (2) IP: route to specific IP address (for ECS awsvpc, on-prem via Direct Connect). (3) Lambda: invoke Lambda function directly. (4) ALB: route to another ALB (for complex architectures). Each target group has: health check settings, routing algorithm (round robin, least outstanding requests), stickiness settings. Multiple target groups enable path-based routing (/api to backend, /static to S3).',
    tags: ['aws', 'alb', 'target-group', 'load-balancing'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between path-based and host-based routing in ALB?',
    options: [
      'Path-based routes by URL path (/api/*); host-based routes by domain name (api.example.com)',
      'They are the same feature with different names in the ALB console following common design principles',
      'Path-based is for HTTP; host-based is for HTTPS only based on production deployment experience following the recommended security practices',
      'Host-based requires Route 53; path-based does not as documented in official AWS best practices following the recommended security practices',
    ],
    correctIndex: 0,
    explanation: 'ALB routing rules: (1) Path-based: route based on URL path. /api/* → API target group, /images/* → S3. (2) Host-based: route based on Host header. api.example.com → API TG, www.example.com → Web TG. (3) Can combine: api.example.com/v1/* → different target. (4) Query string, HTTP headers, source IP conditions also supported. Use cases: path-based for microservices on same domain; host-based for multi-tenant or separate subdomains. Priority order determines which rule matches first.',
    tags: ['aws', 'alb', 'routing', 'path-based', 'host-based'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Users report intermittent 502 Bad Gateway errors from your ALB. How do you troubleshoot?',
    options: [
      'Check target health, security groups, target response timeout, and application logs for crashes',
      '502 errors are always caused by SSL certificate expiration using standard configuration methods according to industry standard guidelines',
      'Restart the ALB using conventional setup approaches based on production deployment experience using established implementation patterns',
      '502 is a client-side error according to industry standard guidelines based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: '502 Bad Gateway troubleshooting: (1) Target health: are targets healthy? Unhealthy targets cause 502. (2) Security groups: ALB SG must allow outbound to targets; target SG must allow inbound from ALB. (3) Response timeout: target not responding within timeout (default 60s). (4) Application crash: app returns invalid response or closes connection. (5) Check ALB access logs and target CloudWatch metrics. (6) Connection issues: target refusing connections or port mismatch. 504 means timeout waiting for response; 502 means target sent invalid response.',
    tags: ['aws', 'alb', 'troubleshooting', '502', 'errors'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you set up ALB to redirect all HTTP traffic to HTTPS?',
    options: [
      'Create HTTP listener on port 80 with redirect action to HTTPS port 443',
      'HTTP to HTTPS redirect requires a separate Nginx proxy in front of ALB',
      'ALB does not support redirects based on production deployment experience as recommended for high availability',
      'Modify security groups following the recommended security practices which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'ALB native HTTP to HTTPS redirect: (1) Create listener on port 443 (HTTPS) with ACM certificate and forward action to target group. (2) Create listener on port 80 (HTTP) with redirect action: protocol HTTPS, port 443, status code 301 (permanent). (3) No need for application code changes or additional infrastructure. (4) Supports path and query string preservation in redirect. Configuration via Console, CLI, or Terraform (aws_lb_listener with default_action type="redirect"). This is the recommended pattern for web applications.',
    tags: ['aws', 'alb', 'https', 'redirect', 'ssl'],
  },

  // ─── API GATEWAY QUESTIONS ────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between API Gateway REST API, HTTP API, and WebSocket API?',
    options: [
      'REST API: full features, higher cost. HTTP API: simpler, lower latency, 70% cheaper. WebSocket: bidirectional real-time',
      'They are all identical in features and pricing following the recommended security practices following common design principles as suggested by cloud architects',
      'REST API is deprecated in favor of HTTP API as recommended for high availability based on production deployment experience following common design principles',
      'WebSocket API is for REST endpoints using established implementation patterns using established implementation patterns based on standard operational procedures',
    ],
    correctIndex: 0,
    explanation: 'API Gateway types: (1) REST API: full-featured (request validation, caching, WAF, usage plans, API keys, request/response transformation). Higher latency, higher cost. (2) HTTP API: streamlined for proxying to Lambda/HTTP backends. 70% cheaper, lower latency, supports JWT authorizers, CORS. Missing: caching, WAF integration, usage plans. (3) WebSocket API: bidirectional real-time communication (chat apps, live dashboards, gaming). Connection-based with @connect, @disconnect, @message routes. Choose HTTP API for simple APIs; REST API for advanced features.',
    tags: ['aws', 'api-gateway', 'rest-api', 'http-api', 'websocket'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is a Lambda authorizer in API Gateway and when would you use it?',
    options: [
      'Custom auth logic in Lambda that validates tokens and returns IAM policy; use for custom auth schemes',
      'Lambda authorizer is the IAM role that Lambda assumes when invoked by API Gateway based on standard operational procedures',
      'It authorizes API Gateway to invoke Lambda functions as documented in official AWS best practices as suggested by cloud architects',
      'Only for WebSocket APIs based on standard operational procedures using established implementation patterns and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Lambda authorizer (custom authorizer) runs authentication/authorization logic: (1) Request-based: receives full request (headers, query params, body). (2) Token-based: receives Authorization header token. Lambda validates credentials (JWT, API key, custom token) and returns IAM policy document with Allow/Deny and resource ARNs. Use cases: (1) Custom auth systems (enterprise SSO), (2) Database-backed API keys, (3) Complex authorization logic. Results can be cached (TTL) to reduce Lambda invocations. Alternative: Cognito authorizer for standard OIDC/JWT.',
    tags: ['aws', 'api-gateway', 'lambda-authorizer', 'authentication', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Your API Gateway returns CORS errors when called from a web browser. How do you fix it?',
    options: [
      'Enable CORS on API Gateway: configure Access-Control-Allow-Origin and other CORS headers in responses',
      'CORS is a browser security bug and cannot be fixed from the server side as recommended for high availability using established implementation patterns',
      'Disable browser security settings following common design principles based on production deployment experience using conventional setup approaches',
      'Use POST instead of GET as recommended for high availability and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'CORS (Cross-Origin Resource Sharing) fix in API Gateway: (1) For HTTP API: enable CORS in API settings, specify allowed origins, methods, headers. (2) For REST API: enable CORS per resource via Console (creates OPTIONS method with headers), or manually configure mock integration for OPTIONS and add headers to method responses. (3) Required headers: Access-Control-Allow-Origin (your domain or *), Access-Control-Allow-Methods, Access-Control-Allow-Headers. (4) Lambda must also return CORS headers in response. Common mistake: enabling CORS on Gateway but not returning headers from Lambda.',
    tags: ['aws', 'api-gateway', 'cors', 'troubleshooting', 'web'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'How do you implement rate limiting and throttling in API Gateway?',
    options: [
      'Use usage plans with API keys for per-client limits, and stage-level throttling for global limits',
      'API Gateway does not support rate limiting using standard configuration methods following common design principles',
      'Rate limiting requires AWS WAF following common design principles as suggested by cloud architects following typical deployment workflows',
      'Only available in HTTP API using standard configuration methods as recommended for high availability following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'API Gateway throttling: (1) Account-level: 10,000 RPS default (soft limit). (2) Stage-level: configure default method throttling (requests per second, burst). (3) Usage plans + API keys: per-client rate limits and quotas. Create usage plan with throttle/quota settings, generate API keys, associate with plan. Clients pass x-api-key header. (4) Per-method overrides: different limits for different endpoints. (5) 429 Too Many Requests returned when throttled. Use cases: protect backend, monetize APIs, prevent abuse. WAF adds additional DDoS protection layer.',
    tags: ['aws', 'api-gateway', 'throttling', 'rate-limiting', 'usage-plans'],
  },

  // ─── AUTO SCALING QUESTIONS ───────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between a Launch Template and Launch Configuration in Auto Scaling?',
    options: [
      'Launch Template is newer, supports versioning, multiple instance types, and Spot; Launch Configuration is legacy',
      'They are identical features with different names as documented in official AWS best practices using established implementation patterns',
      'Launch Configuration supports more features using standard configuration methods as suggested by cloud architects as suggested by cloud architects',
      'Launch Template is for EKS only implementing typical operational patterns and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Launch Template (recommended): (1) Supports versioning (update template, roll out gradually). (2) Multiple instance types in one template (capacity-optimized Spot). (3) Spot and On-Demand mix. (4) Network interface configurations. (5) T2/T3 Unlimited CPU credits. (6) Required for new features. Launch Configuration (legacy): (1) Immutable — must create new configuration for changes. (2) Single instance type. (3) Limited features. (4) Still supported but not recommended. AWS recommends migrating to Launch Templates for all new ASGs.',
    tags: ['aws', 'auto-scaling', 'launch-template', 'launch-configuration'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are the different types of Auto Scaling policies and when would you use each?',
    options: [
      'Target tracking (maintain metric), step scaling (thresholds), simple scaling (single adjustment), scheduled (time-based)',
      'Auto Scaling only supports manual scaling policies implementing typical operational patterns and this approach is commonly used in enterprise settings',
      'All scaling policies work identically following common design principles and this approach is commonly used in enterprise settings',
      'Scaling policies are only for ECS based on production deployment experience using established implementation patterns using conventional setup approaches',
    ],
    correctIndex: 0,
    explanation: 'Auto Scaling policy types: (1) Target tracking: maintain a metric at target value (e.g., CPU at 50%). ASG adjusts capacity automatically. Simplest, recommended for most cases. (2) Step scaling: define steps — scale +2 at 60% CPU, +4 at 80% CPU. More control than target tracking. (3) Simple scaling: single adjustment with cooldown. Legacy, use step scaling instead. (4) Scheduled: scale at specific times (increase capacity before known traffic peaks, reduce at night). (5) Predictive: ML-based forecasting (scales proactively based on patterns). Combine multiple policies for optimal scaling.',
    tags: ['aws', 'auto-scaling', 'scaling-policies', 'target-tracking'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the cooldown period in Auto Scaling and why is it important?',
    options: [
      'Pause between scaling activities to let metrics stabilize; prevents rapid scale up/down oscillation',
      'Time before ASG starts for the first time after creation using standard configuration methods which follows AWS recommended architecture patterns',
      'Duration instances run before automatic termination implementing typical operational patterns following common design principles',
      'Cooldown is deprecated as recommended for high availability based on production deployment experience following common design principles',
    ],
    correctIndex: 0,
    explanation: 'Cooldown period: time after a scaling activity during which ASG ignores further alarms. Default: 300 seconds. Purpose: allow newly launched instances to start handling traffic and metrics to stabilize before making more scaling decisions. Without cooldown: ASG might scale up, see still-high CPU (new instances not yet active), scale up again unnecessarily. Target tracking policies have built-in warmup period (scale-out warmup) instead of cooldown. Best practice: set cooldown based on instance startup time. Reduce for fast-starting containers; increase for slow-starting apps.',
    tags: ['aws', 'auto-scaling', 'cooldown', 'stabilization'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your ASG keeps launching and terminating instances in a loop. What is the likely cause?',
    options: [
      'Health check failure: instances fail health check, get terminated, new ones launch, also fail. Check health check path and app startup time',
      'This is normal ASG behavior and cannot be changed following typical deployment workflows as suggested by cloud architects following the recommended security practices',
      'ASG has reached its maximum capacity limit based on standard operational procedures according to industry standard guidelines according to industry standard guidelines',
      'AWS is having a service outage based on standard operational procedures using established implementation patterns according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'ASG launch/terminate loop troubleshooting: (1) Health check failing: app not responding on health check path/port by the time health check runs. Increase health check grace period. (2) Instance immediately unhealthy: check EC2 status checks, security groups, user data script errors. (3) ELB health check: verify ALB/NLB health check path returns 200, check target group settings. (4) Launch failures: AMI deleted, IAM role issues, subnet has no IPs. Check ASG Activity History for detailed error messages. Common fix: increase health check grace period to allow application warm-up.',
    tags: ['aws', 'auto-scaling', 'troubleshooting', 'health-check', 'loop'],
  },

  // ─── ROUTE 53 QUESTIONS ───────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between a Route 53 Alias record and a CNAME record?',
    options: [
      'Alias: free, works at zone apex, resolves to AWS resource IPs. CNAME: costs per query, cannot be at zone apex',
      'They are identical in function and cost as suggested by cloud architects using conventional setup approaches based on standard operational procedures',
      'CNAME is recommended over Alias for all use cases implementing typical operational patterns as documented in official AWS best practices',
      'Alias only works with EC2 following the recommended security practices following the recommended security practices based on standard operational procedures',
    ],
    correctIndex: 0,
    explanation: 'Route 53 Alias vs CNAME: (1) Alias: AWS extension, free, can be used at zone apex (example.com), resolves directly to AWS resource IP (no extra DNS lookup), works with CloudFront, ALB, S3, API Gateway, etc. (2) CNAME: standard DNS, charged per query, cannot be at zone apex (only subdomains like www.example.com), adds extra DNS resolution hop. Use Alias for: zone apex records pointing to AWS resources, cost savings. Use CNAME for: pointing to non-AWS resources or when Alias is not available.',
    tags: ['aws', 'route53', 'alias', 'cname', 'dns'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What routing policies does Route 53 support and when would you use weighted routing?',
    options: [
      'Simple, weighted, latency, failover, geolocation, multivalue, geoproximity. Weighted: distribute traffic by percentage',
      'Route 53 only supports round-robin routing following common design principles according to industry standard guidelines following common design principles',
      'Weighted routing requires CloudFront using conventional setup approaches as documented in official AWS best practices which follows AWS recommended architecture patterns',
      'Routing policies are only for private hosted zones and this approach is commonly used in enterprise settings implementing typical operational patterns',
    ],
    correctIndex: 0,
    explanation: 'Route 53 routing policies: (1) Simple: single resource, no health checks. (2) Weighted: distribute traffic by weight (70/30 split for canary). (3) Latency: route to region with lowest latency. (4) Failover: active-passive with health checks. (5) Geolocation: route by user location (country/continent). (6) Geoproximity: route by geographic proximity with bias. (7) Multivalue: return multiple healthy IPs. Weighted routing use cases: blue-green deployment (shift traffic gradually), A/B testing, gradual migrations between regions.',
    tags: ['aws', 'route53', 'routing-policy', 'weighted', 'dns'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'You made a DNS change in Route 53 but users are still hitting the old server. Why?',
    options: [
      'DNS caching: clients and resolvers cache records until TTL expires. Wait for TTL or lower TTL before changes',
      'Route 53 changes require AWS support approval before taking effect and this approach is commonly used in enterprise settings',
      'DNS changes are instant and this indicates a different issue based on production deployment experience implementing typical operational patterns',
      'Route 53 has a 24-hour propagation delay and this approach is commonly used in enterprise settings using conventional setup approaches',
    ],
    correctIndex: 0,
    explanation: 'DNS propagation delay causes: (1) TTL (Time To Live): resolvers cache records for TTL duration. If TTL was 86400 (24h), change takes up to 24h to propagate. (2) Client caching: browsers, OS cache DNS independently. (3) ISP resolvers may not respect TTL. Solutions: (1) Lower TTL before planned changes (e.g., 60 seconds), wait for old TTL to expire, make change. (2) After change, raise TTL back. (3) Flush local DNS: ipconfig /flushdns (Windows), sudo dscacheutil -flushcache (Mac). Route 53 changes apply within 60 seconds at AWS; propagation depends on caching.',
    tags: ['aws', 'route53', 'dns', 'ttl', 'propagation'],
  },

  // ─── EVENTBRIDGE QUESTIONS ────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon EventBridge and how does it differ from SNS and SQS?',
    options: [
      'EventBridge: event routing with filtering and transformation. SNS: pub/sub fan-out. SQS: message queue with persistence',
      'EventBridge, SNS, and SQS are identical services using established implementation patterns following common design principles using standard configuration methods',
      'EventBridge replaces both SNS and SQS based on standard operational procedures following common design principles based on standard operational procedures',
      'EventBridge is only for CloudWatch Events based on standard operational procedures as documented in official AWS best practices which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'EventBridge vs SNS vs SQS: (1) EventBridge: serverless event bus, content-based filtering on event attributes, schema registry, transformation, 35+ AWS service integrations, cross-account events. Use for: event-driven architectures, decoupling, workflows. (2) SNS: pub/sub messaging, fan-out to multiple subscribers (SQS, Lambda, HTTP, email). Use for: notifications, fan-out patterns. (3) SQS: message queue, persistence, dead-letter queues, FIFO ordering. Use for: decoupling, buffering, async processing. EventBridge is the evolution of CloudWatch Events with more features.',
    tags: ['aws', 'eventbridge', 'sns', 'sqs', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'How do you trigger a Lambda function when an EC2 instance state changes using EventBridge?',
    options: [
      'Create EventBridge rule with pattern matching EC2 state-change events, set Lambda as target',
      'EC2 must push events to Lambda directly and this approach is commonly used in enterprise settings which follows AWS recommended architecture patterns',
      'Use CloudWatch Logs only following common design principles based on standard operational procedures following typical deployment workflows',
      'Polling EC2 API is required based on standard operational procedures using established implementation patterns',
    ],
    correctIndex: 0,
    explanation: 'EventBridge rule for EC2 state changes: (1) Source: aws.ec2. (2) Detail-type: "EC2 Instance State-change Notification". (3) Pattern: {"source": ["aws.ec2"], "detail-type": ["EC2 Instance State-change Notification"], "detail": {"state": ["stopped", "terminated"]}}. (4) Target: Lambda function ARN. When EC2 instance changes state, EventBridge routes matching events to Lambda. Use cases: audit logging, automated remediation, Slack notifications. Lambda receives event with instance-id, state, and timestamp.',
    tags: ['aws', 'eventbridge', 'ec2', 'lambda', 'event-driven'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you create a scheduled cron job using EventBridge?',
    options: [
      'Create EventBridge rule with schedule expression (rate or cron), set Lambda/ECS/Step Functions as target',
      'EventBridge does not support scheduled events using standard configuration methods as documented in official AWS best practices',
      'Use EC2 with crontab only using established implementation patterns using standard configuration methods as recommended for high availability',
      'CloudWatch Alarms only using standard configuration methods and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EventBridge scheduled rules: (1) Rate expression: rate(5 minutes), rate(1 hour), rate(1 day). (2) Cron expression: cron(0 12 * * ? *) for daily at noon UTC. Format: cron(min hour day month day-of-week year). (3) Target: Lambda, ECS task, Step Functions, SNS, SQS, etc. Use cases: daily reports, cleanup jobs, health checks, data synchronization. Previously CloudWatch Events scheduled rules; now EventBridge. Serverless alternative to EC2 cron jobs. Supports retry policies and dead-letter queues for failed invocations.',
    tags: ['aws', 'eventbridge', 'scheduled', 'cron', 'serverless'],
  },

  // ─── EBS QUESTIONS ────────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What are the main EBS volume types and their use cases?',
    options: [
      'gp3/gp2: general SSD. io2/io1: high IOPS SSD. st1: throughput HDD. sc1: cold HDD',
      'EBS has only one volume type following common design principles and this approach is commonly used in enterprise settings',
      'All EBS types have identical performance according to industry standard guidelines using standard configuration methods',
      'gp3 is slower than gp2 based on standard operational procedures as recommended for high availability',
    ],
    correctIndex: 0,
    explanation: 'EBS volume types: (1) gp3: general purpose SSD, 3000 IOPS baseline, 125 MB/s, independently scale IOPS/throughput. Best default choice. (2) gp2: older general purpose, IOPS scales with size (3 IOPS/GB). (3) io2/io1: provisioned IOPS SSD, up to 64,000 IOPS. For databases requiring consistent high IOPS. (4) st1: throughput-optimized HDD, 500 MB/s, for big data, log processing. (5) sc1: cold HDD, lowest cost, infrequent access archives. Choose based on: IOPS needs (io2 for databases), throughput needs (st1 for streaming), cost (sc1 for cold data), general purpose (gp3).',
    tags: ['aws', 'ebs', 'volume-types', 'storage'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between gp2 and gp3 EBS volumes?',
    options: [
      'gp3: baseline 3000 IOPS, independent IOPS/throughput scaling, 20% cheaper. gp2: IOPS tied to size (3 IOPS/GB)',
      'gp2 is newer and faster than gp3 following the recommended security practices implementing typical operational patterns',
      'They are identical except for the name following the recommended security practices implementing typical operational patterns',
      'gp3 requires io2 for high IOPS which follows AWS recommended architecture patterns using conventional setup approaches and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'gp2 vs gp3: (1) gp2: IOPS scales with volume size (3 IOPS per GB, min 100, max 16,000). 1TB volume = 3,000 IOPS. Throughput scales with IOPS. Must increase size for more performance. (2) gp3: baseline 3,000 IOPS and 125 MB/s regardless of size. Can independently provision up to 16,000 IOPS and 1,000 MB/s. 20% cheaper than gp2 at baseline. Recommendation: use gp3 for new volumes, migrate existing gp2 for cost savings and flexibility. gp3 decouples performance from capacity.',
    tags: ['aws', 'ebs', 'gp2', 'gp3', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Can you change an EBS volume type or increase size without downtime?',
    options: [
      'Yes, use EBS Elastic Volumes to modify type, size, or IOPS online without detaching',
      'Volume changes always require stopping the EC2 instance according to industry standard guidelines',
      'Volume type cannot be changed after creation using established implementation patterns based on production deployment experience',
      'Only size can be changed, not type implementing typical operational patterns implementing typical operational patterns',
    ],
    correctIndex: 0,
    explanation: 'EBS Elastic Volumes enable online modifications: (1) Increase size: expand volume, then extend filesystem (resize2fs for ext4, xfs_growfs for xfs). (2) Change type: gp2 to gp3, gp3 to io2, etc. (3) Adjust IOPS/throughput for gp3/io2. Process: modify volume via Console/CLI → optimization in progress → completed. No detach required, no downtime. Limitations: cannot decrease size (only increase), some older instance types have restrictions, modification cooldown (6 hours between modifications on same volume).',
    tags: ['aws', 'ebs', 'elastic-volumes', 'modification', 'online'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Your database on EC2 experiences high I/O latency on EBS. What volume type should you switch to?',
    options: [
      'io2 or io2 Block Express for provisioned high IOPS with consistent low latency',
      'sc1 for better database performance which follows AWS recommended architecture patterns as documented in official AWS best practices',
      'st1 for databases using standard configuration methods using conventional setup approaches',
      'gp2 smaller size based on standard operational procedures which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'For database I/O performance: (1) io2: provisioned IOPS SSD, up to 64,000 IOPS, 99.999% durability, consistent single-digit millisecond latency. (2) io2 Block Express: up to 256,000 IOPS, 4,000 MB/s, sub-millisecond latency. For most demanding databases. (3) Provision IOPS to match database requirements (check CloudWatch EBS metrics for current IOPS). (4) Consider: RAID 0 for aggregate throughput, RDS/Aurora for managed databases, instance storage (i3/d2) for ephemeral high-IOPS needs. st1/sc1 are HDD — wrong for database IOPS. gp2/gp3 may work for less demanding databases.',
    tags: ['aws', 'ebs', 'io2', 'database', 'performance', 'iops'],
  },

  // ─── EFS QUESTIONS ────────────────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon EFS and how does it differ from EBS?',
    options: [
      'EFS: shared NFS filesystem for multiple instances across AZs. EBS: block storage attached to single instance',
      'EFS and EBS are identical in functionality implementing typical operational patterns following the recommended security practices',
      'EBS supports multiple instances; EFS does not as recommended for high availability based on standard operational procedures',
      'EFS is block storage; EBS is file storage following the recommended security practices based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'EFS vs EBS: (1) EFS: managed NFS (NFSv4), shared across multiple EC2 instances simultaneously, spans all AZs in region, petabyte scale, pay per GB used, automatic scaling. (2) EBS: block storage, typically attached to single instance (Multi-Attach for io2 only), single AZ, fixed provisioned size. Use EFS for: shared file storage, CMS, home directories, container persistent storage. Use EBS for: boot volumes, databases, single-instance applications. EFS is ~3x more expensive per GB than EBS gp3.',
    tags: ['aws', 'efs', 'ebs', 'storage', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What throughput modes does EFS support?',
    options: [
      'Bursting (scales with size), Provisioned (fixed throughput), Elastic (auto-scales to workload)',
      'EFS has only one throughput mode as documented in official AWS best practices following typical deployment workflows',
      'Throughput cannot be configured based on standard operational procedures based on standard operational procedures',
      'Provisioned mode is deprecated following typical deployment workflows following the recommended security practices',
    ],
    correctIndex: 0,
    explanation: 'EFS throughput modes: (1) Bursting: throughput scales with filesystem size. 50 KB/s per GB baseline, burst up to 100 MB/s. Good for spiky workloads with small-to-medium filesystems. (2) Provisioned: specify throughput independently of size. 1-1024 MB/s. Good for high-throughput small filesystems. (3) Elastic (recommended): automatically scales throughput based on workload, up to 3 GB/s read, 1 GB/s write. Pay only for throughput used. Best for unpredictable workloads. Choose based on: predictable high throughput → Provisioned; variable workload → Elastic; cost-sensitive → Bursting.',
    tags: ['aws', 'efs', 'throughput', 'bursting', 'elastic'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Multiple EC2 instances need access to the same shared filesystem. Should you use EBS or EFS?',
    options: [
      'EFS: designed for multi-instance shared access via NFS across AZs',
      'EBS: attach same volume to multiple instances following common design principles',
      'Neither supports shared access using established implementation patterns following common design principles',
      'Use S3 for shared filesystem following common design principles following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'For shared filesystem: EFS is the correct choice. (1) EFS: NFS-based, designed for concurrent access from thousands of instances across AZs. Mount via: mount -t nfs4 fs-xxx.efs.region.amazonaws.com:/ /mnt. (2) EBS Multi-Attach: only io1/io2 volumes, limited to 16 instances in same AZ, requires cluster-aware filesystem (not standard ext4). Not practical for most shared storage needs. (3) S3: object storage, not a POSIX filesystem (though s3fs exists, not recommended for production). (4) FSx for Lustre/Windows: alternative managed filesystems for specific use cases.',
    tags: ['aws', 'efs', 'ebs', 'shared-storage', 'multi-instance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you mount EFS in an ECS Fargate task?',
    options: [
      'Configure EFS volume in task definition with filesystem ID and access point, mount in container definition',
      'Fargate does not support EFS using standard configuration methods based on production deployment experience as recommended for high availability',
      'Use EBS only for Fargate as documented in official AWS best practices and this approach is commonly used in enterprise settings',
      'Requires EC2 launch type following typical deployment workflows implementing typical operational patterns based on standard operational procedures',
    ],
    correctIndex: 0,
    explanation: 'EFS with Fargate: (1) Task definition: add volume with efsVolumeConfiguration containing fileSystemId, optionally accessPointId, transitEncryption. (2) Container definition: add mountPoints referencing the volume and container path. (3) Networking: Fargate task and EFS must be in same VPC, security groups must allow NFS (port 2049). (4) IAM: task role needs elasticfilesystem:ClientMount permissions. (5) Use access points for different apps with separate directories and POSIX permissions. Common use case: shared data between container replicas, persistent storage for stateless containers.',
    tags: ['aws', 'efs', 'fargate', 'ecs', 'persistent-storage'],
  },

  // ─── LAMBDA QUESTIONS (REMAINING) ─────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is AWS Lambda and how does it work?',
    options: [
      'Serverless compute that runs code in response to events; you pay only for execution time, no server management',
      'Lambda is a container orchestration service similar to ECS but with automatic scaling based on traffic patterns',
      'Lambda requires provisioning EC2 instances behind the scenes which you manage through the Lambda console',
      'Lambda only works with Python and Node.js and cannot run other programming languages',
    ],
    correctIndex: 0,
    explanation: 'AWS Lambda is serverless compute: (1) Event-driven: triggered by API Gateway, S3, DynamoDB, EventBridge, SQS, etc. (2) No server management: AWS handles provisioning, scaling, patching. (3) Pay per use: billed per request and execution duration (GB-seconds). (4) Auto-scaling: handles 1 to thousands of concurrent executions. (5) Supports: Node.js, Python, Java, Go, .NET, Ruby, custom runtimes. (6) Max execution: 15 minutes. (7) Memory: 128MB to 10GB (CPU scales with memory). Use cases: APIs, data processing, automation, backends.',
    tags: ['aws', 'lambda', 'serverless', 'fundamentals'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is a Lambda cold start and how do you reduce it?',
    options: [
      'Cold start is initialization delay when new execution environment is created; reduce with Provisioned Concurrency, smaller packages, faster runtimes',
      'Cold start cannot be reduced and is always exactly 10 seconds regardless of configuration or runtime settings and this approach is commonly used in enterprise settings',
      'Cold start only affects Python functions and does not impact Node.js or Java runtimes in any way implementing typical operational patterns implementing typical operational patterns',
      'Provisioned Concurrency eliminates cold starts but costs 10x more than regular Lambda invocations always and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Cold start occurs when Lambda creates new execution environment: (1) Download code, start runtime, run init code. (2) Duration: 100ms-2s depending on runtime, package size, VPC. (3) Reduce cold starts: Provisioned Concurrency (keeps environments warm), smaller deployment packages, avoid VPC if not needed, use lighter runtimes (Python/Node.js vs Java), lazy-load dependencies. (4) Warm invocations: reuse existing environment, much faster. (5) SnapStart (Java): pre-initializes and snapshots environment. Monitor cold starts via CloudWatch Logs (Init Duration).',
    tags: ['aws', 'lambda', 'cold-start', 'performance', 'provisioned-concurrency'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are Lambda layers and when would you use them?',
    options: [
      'Layers are ZIP archives with libraries/dependencies shared across functions; use for common code, large dependencies, custom runtimes',
      'Layers are only for storing Lambda function code and cannot contain external libraries or dependencies and this approach is commonly used in enterprise settings',
      'Each Lambda function can only use one layer and layers cannot be shared between functions in an account implementing typical operational patterns',
      'Layers are deprecated in favor of container images and should not be used for new Lambda functions using conventional setup approaches using conventional setup approaches',
    ],
    correctIndex: 0,
    explanation: 'Lambda layers: (1) ZIP archive containing libraries, custom runtimes, or other dependencies. (2) Attached to function, extracted to /opt directory. (3) Benefits: share common code across functions, separate dependencies from function code, reduce deployment package size. (4) Up to 5 layers per function, 250MB total unzipped limit. (5) Use cases: AWS SDK updates, shared utilities, Pandas/NumPy for Python, custom runtimes (PHP, Rust). (6) Version layers independently from functions. (7) AWS provides layers: AWS Parameters and Secrets Extension, etc.',
    tags: ['aws', 'lambda', 'layers', 'dependencies', 'reusability'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does Lambda scaling work? What is concurrency and reserved concurrency?',
    options: [
      'Lambda auto-scales by creating execution environments; concurrency is simultaneous executions; reserved concurrency guarantees capacity',
      'Lambda requires manual scaling configuration similar to EC2 Auto Scaling groups with min and max instances according to industry standard guidelines',
      'Concurrency and reserved concurrency are identical concepts with no functional difference between them as suggested by cloud architects following typical deployment workflows',
      'Reserved concurrency is only available in Lambda@Edge and cannot be used with standard Lambda functions as suggested by cloud architects following the recommended security practices',
    ],
    correctIndex: 0,
    explanation: 'Lambda scaling: (1) Auto-scales by creating more execution environments for concurrent requests. (2) Concurrency: number of requests being handled simultaneously. (3) Account limit: 1,000 concurrent executions default (can increase). (4) Reserved concurrency: guarantee N environments for a function, also caps maximum. (5) Provisioned concurrency: pre-initialized environments (eliminates cold starts, costs more). (6) Burst limits: 500-3000 immediate concurrent executions depending on region, then 500/minute increase. (7) Throttling: returns 429 when limit reached.',
    tags: ['aws', 'lambda', 'scaling', 'concurrency', 'reserved-concurrency'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is Lambda@Edge and how does it differ from CloudFront Functions?',
    options: [
      'Lambda@Edge runs at CloudFront edge locations with full Lambda features; CloudFront Functions are lighter, faster, cheaper for simple tasks',
      'Lambda@Edge and CloudFront Functions are identical services with the same capabilities and pricing model using established implementation patterns following typical deployment workflows',
      'CloudFront Functions can run for up to 15 minutes while Lambda@Edge is limited to 5 seconds maximum which follows AWS recommended architecture patterns',
      'Lambda@Edge only works with S3 origins and cannot be used with ALB or custom origins in CloudFront as documented in official AWS best practices using established implementation patterns',
    ],
    correctIndex: 0,
    explanation: 'Lambda@Edge vs CloudFront Functions: (1) Lambda@Edge: full Lambda, up to 5s (viewer) or 30s (origin), 128MB-10GB memory, supports all Lambda features, higher latency/cost. (2) CloudFront Functions: lightweight JavaScript, sub-millisecond, 2MB memory, viewer request/response only, 1/6th cost of Lambda@Edge. Use Lambda@Edge for: complex logic, external API calls, body manipulation. Use CloudFront Functions for: URL rewrites, header manipulation, simple redirects, A/B testing, JWT validation. Both run at edge locations.',
    tags: ['aws', 'lambda', 'lambda-edge', 'cloudfront-functions', 'edge-computing'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are Lambda destinations vs Dead Letter Queue (DLQ)?',
    options: [
      'Destinations route async results (success/failure) to targets; DLQ only captures failures. Destinations are newer and more flexible',
      'DLQ and destinations are identical features and using both simultaneously is required for error handling according to industry standard guidelines',
      'Destinations only work with synchronous invocations while DLQ works with asynchronous invocations only according to industry standard guidelines',
      'Lambda destinations are deprecated in favor of DLQ and should not be used in new Lambda functions as suggested by cloud architects following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'Lambda destinations (recommended): (1) Route async invocation results to SNS, SQS, Lambda, or EventBridge. (2) Separate destinations for success and failure. (3) Includes full event, response, and metadata. DLQ (older): (1) Only for failures. (2) Only SQS or SNS. (3) Only includes original event. Use destinations for: event-driven workflows, capturing both success/failure, rich metadata. Use DLQ for: simple failure capture, existing workflows. Both require async invocation (S3 triggers, EventBridge, etc.).',
    tags: ['aws', 'lambda', 'destinations', 'dlq', 'error-handling'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'How do you give Lambda access to a VPC resource like RDS?',
    options: [
      'Configure VPC, subnets, and security groups in Lambda; ensure Lambda SG can reach RDS SG; use private subnets with NAT for internet',
      'Lambda automatically has access to all VPC resources without any configuration needed from the developer and this approach is commonly used in enterprise settings',
      'VPC access requires Lambda to be deployed on dedicated EC2 instances using Lambda container deployment mode as recommended for high availability',
      'Lambda cannot access VPC resources and requires API Gateway as a proxy for all database connections as recommended for high availability which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'Lambda VPC access: (1) Configure: VPC ID, subnet IDs (use private subnets), security group. (2) Lambda creates ENIs in your subnets (needs ec2:CreateNetworkInterface permission). (3) Security groups: Lambda SG must allow outbound to RDS port; RDS SG must allow inbound from Lambda SG. (4) Internet access: Lambda in VPC loses internet access; add NAT Gateway for external APIs. (5) AWS services: use VPC endpoints (S3, DynamoDB, Secrets Manager) to avoid NAT costs. (6) Hyperplane ENIs: shared ENIs reduce cold start impact of VPC.',
    tags: ['aws', 'lambda', 'vpc', 'rds', 'networking'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Lambda function is timing out. How do you debug?',
    options: [
      'Check CloudWatch Logs for where execution stops, increase timeout, check external calls (DB, API), verify VPC/NAT if applicable',
      'Lambda timeouts are always caused by infinite loops in code and require complete function rewrite according to industry standard guidelines',
      'Increase memory to maximum 10GB as this automatically increases timeout to 15 minutes based on production deployment experience using standard configuration methods',
      'Lambda timeouts cannot be debugged and functions should be redeployed with different code as documented in official AWS best practices following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'Lambda timeout debugging: (1) CloudWatch Logs: add logging to identify where execution stops. (2) Timeout setting: default 3s, max 15min — increase if legitimate long processing. (3) External calls: database queries, API calls taking too long. (4) VPC issues: if in VPC, check NAT Gateway, security groups, VPC endpoints. (5) Cold starts: init code taking too long. (6) X-Ray: enable tracing to visualize latency breakdown. (7) Connection pooling: reuse connections outside handler. (8) Async: consider Step Functions for long workflows. Common cause: external service latency, missing VPC connectivity.',
    tags: ['aws', 'lambda', 'timeout', 'debugging', 'troubleshooting'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Lambda in VPC cannot reach the internet. What do you configure?',
    options: [
      'Add NAT Gateway in public subnet, update private subnet route table to route 0.0.0.0/0 through NAT',
      'Lambda in VPC automatically has internet access through the VPC Internet Gateway without configuration',
      'Internet access is not possible for Lambda in VPC and requires removing VPC configuration entirely',
      'Configure Internet Gateway directly on the Lambda function security group inbound rules',
    ],
    correctIndex: 0,
    explanation: 'Lambda VPC internet access: (1) Lambda in VPC uses private IP, cannot reach internet directly. (2) Solution: NAT Gateway in public subnet (with IGW route). (3) Private subnet route table: 0.0.0.0/0 → NAT Gateway. (4) Public subnet route table: 0.0.0.0/0 → Internet Gateway. (5) Alternatives: VPC endpoints for AWS services (cheaper, no NAT needed). (6) NAT Gateway costs: ~$0.045/hour + data processing. (7) Multi-AZ: deploy NAT Gateway per AZ for high availability. Common mistake: placing Lambda in public subnet does not give internet access (no public IP assigned).',
    tags: ['aws', 'lambda', 'vpc', 'nat-gateway', 'internet-access'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Lambda is throttling in production. What are your options?',
    options: [
      'Request limit increase, use reserved concurrency, implement retries with backoff, consider SQS buffer, optimize function duration',
      'Lambda throttling cannot be resolved and indicates the function should be migrated to EC2 immediately as recommended for high availability using conventional setup approaches',
      'Throttling is only caused by code errors and fixing bugs will automatically resolve all throttling issues which follows AWS recommended architecture patterns',
      'Reserved concurrency must be set to unlimited to prevent any throttling from occurring in production using standard configuration methods based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Lambda throttling solutions: (1) Request limit increase: Service Quotas → concurrent executions (default 1000). (2) Reserved concurrency: guarantee capacity for critical functions. (3) Provisioned concurrency: pre-warm environments, also prevents throttling. (4) SQS buffer: queue requests, Lambda polls at controlled rate. (5) Optimize duration: faster functions = more throughput with same concurrency. (6) Retries: SDK automatic retries with exponential backoff. (7) Monitor: CloudWatch Throttles metric, set alarms. (8) Architecture: consider Step Functions, EventBridge for event distribution.',
    tags: ['aws', 'lambda', 'throttling', 'scaling', 'production'],
  },

  // ─── FARGATE QUESTIONS (REMAINING) ────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is Fargate and how does it differ from ECS on EC2?',
    options: [
      'Fargate is serverless containers — no EC2 management, pay per task resources. EC2 launch type requires managing instances',
      'Fargate and ECS EC2 are identical except Fargate costs exactly half the price of running equivalent EC2 instances using established implementation patterns',
      'Fargate requires Kubernetes knowledge while ECS EC2 launch type uses Docker Swarm for orchestration according to industry standard guidelines',
      'EC2 launch type is serverless while Fargate requires manual EC2 instance provisioning and management following the recommended security practices',
    ],
    correctIndex: 0,
    explanation: 'Fargate vs ECS EC2: (1) Fargate: serverless, no EC2 to manage, pay per task (vCPU + memory), AWS handles infrastructure. (2) ECS EC2: you manage EC2 instances, more control, potentially cheaper for steady workloads. Fargate benefits: no patching, no capacity planning, per-second billing. Fargate limitations: no GPU support, less customization, slightly higher cost. Choose Fargate for: variable workloads, small teams, quick deployment. Choose EC2: cost optimization, GPU needs, specific instance requirements.',
    tags: ['aws', 'fargate', 'ecs', 'serverless', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'How does Fargate pricing work?',
    options: [
      'Pay per second for vCPU and memory provisioned to tasks; no charges for underlying infrastructure',
      'Fargate has a flat monthly fee regardless of usage similar to EC2 Reserved Instances pricing model',
      'Pricing is based only on memory allocation and vCPU configuration has no impact on costs',
      'Fargate is free tier eligible with unlimited usage for the first year of AWS account creation',
    ],
    correctIndex: 0,
    explanation: 'Fargate pricing: (1) Per-second billing (1 minute minimum). (2) vCPU: ~$0.04/vCPU/hour. (3) Memory: ~$0.004/GB/hour. (4) No EC2 costs — infrastructure is abstracted. (5) Fargate Spot: up to 70% discount for interruptible workloads. (6) Savings Plans: up to 50% with 1/3-year commitment. (7) Comparison: often 30-50% more expensive than well-utilized EC2, but saves operational overhead. Calculate: sum task vCPU hours + memory GB hours. Storage: 20GB ephemeral included, additional EFS charges separately.',
    tags: ['aws', 'fargate', 'pricing', 'cost'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is Fargate Spot and when would you use it?',
    options: [
      'Fargate Spot runs tasks on spare capacity at up to 70% discount; use for fault-tolerant, interruptible workloads',
      'Fargate Spot provides guaranteed capacity with priority access and costs more than regular Fargate tasks following common design principles',
      'Spot is only available for ECS EC2 launch type and is not supported in Fargate deployment mode based on standard operational procedures',
      'Fargate Spot tasks run indefinitely once started and cannot be interrupted by AWS for any reason as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'Fargate Spot: (1) Runs on spare AWS capacity. (2) Up to 70% discount vs regular Fargate. (3) Can be interrupted with 2-minute warning (SIGTERM). (4) Use for: batch processing, CI/CD builds, data processing, dev/test environments. (5) Configure: capacity provider strategy with FARGATE_SPOT. (6) Mix with regular Fargate: base=1 FARGATE + weight=4 FARGATE_SPOT for resilience. (7) Not for: production web services, databases, stateful workloads. Handle interruption: checkpoint progress, use SQS for work queue.',
    tags: ['aws', 'fargate', 'spot', 'cost-optimization'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does networking work in Fargate with awsvpc network mode?',
    options: [
      'Each task gets its own ENI with private IP in your VPC; configure subnets and security groups at task level',
      'Fargate tasks share a single IP address and port mapping is required similar to Docker bridge networking as suggested by cloud architects',
      'awsvpc mode is optional in Fargate and tasks can also use host networking mode for better performance using standard configuration methods',
      'Fargate networking is managed entirely by AWS and you cannot configure subnets or security groups using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'Fargate networking (awsvpc): (1) Required mode for Fargate (no host/bridge options). (2) Each task gets dedicated ENI in your VPC. (3) Task has its own private IP address. (4) Configure: subnet IDs, security groups at service/task level. (5) Benefits: full VPC integration, security group per task, no port conflicts. (6) Public IP: assignable if in public subnet (for internet access without NAT). (7) Service discovery: integrates with Cloud Map for DNS-based discovery. (8) ENI limits: consider subnet size for IP addresses.',
    tags: ['aws', 'fargate', 'networking', 'awsvpc', 'eni'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you give a Fargate task access to AWS services?',
    options: [
      'Attach IAM task role to the task definition; the task assumes this role to access S3, DynamoDB, Secrets Manager, etc.',
      'Fargate tasks use the ECS cluster IAM role for all AWS service access and cannot have individual permissions based on standard operational procedures',
      'AWS credentials must be passed as environment variables in the task definition for service access implementing typical operational patterns',
      'Fargate tasks cannot access other AWS services directly and require API Gateway as an intermediary and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Fargate AWS access via IAM roles: (1) Task role: permissions your application uses at runtime (S3, DynamoDB, SQS access). (2) Specified in task definition taskRoleArn. (3) Container credential provider: automatically injects temporary credentials. (4) Application uses AWS SDK with default credential chain. (5) Principle of least privilege: grant only needed permissions. (6) Separate from execution role (which is for ECS agent to pull images, push logs). Example: task role allows s3:GetObject for specific bucket, secretsmanager:GetSecretValue for app secrets.',
    tags: ['aws', 'fargate', 'iam', 'task-role', 'permissions'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'Fargate task fails to pull image. How do you debug?',
    options: [
      'Check execution role has ECR permissions, verify VPC endpoints or NAT for ECR access, confirm image URI and tag exist',
      'Image pull failures are always caused by corrupted images and require rebuilding the Docker image based on production deployment experience',
      'Fargate can only pull images from Docker Hub and ECR images require ECS EC2 launch type which follows AWS recommended architecture patterns',
      'Restart the ECS cluster to refresh the container registry authentication tokens automatically based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Fargate image pull troubleshooting: (1) Execution role: needs ecr:GetAuthorizationToken, ecr:BatchGetImage, ecr:GetDownloadUrlForLayer. (2) Networking: private subnet needs NAT Gateway or VPC endpoints (ecr.api, ecr.dkr, s3) to reach ECR. (3) Image URI: verify account ID, region, repository name, tag. (4) Repository exists: confirm image was pushed successfully. (5) Repository policy: allows execution role principal. (6) Check Stopped tasks in ECS console for detailed error. (7) Docker Hub: check rate limits, use ECR pull-through cache.',
    tags: ['aws', 'fargate', 'ecr', 'troubleshooting', 'image-pull'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'THEORY' as const,
    question: 'When would you choose Fargate over EC2 launch type for ECS?',
    options: [
      'Choose Fargate for: variable workloads, small teams, no infra management, quick scaling. Choose EC2: cost optimization, GPU, specific instance needs',
      'Fargate should always be chosen over EC2 launch type as it provides better performance in all scenarios based on standard operational procedures using established implementation patterns',
      'EC2 launch type is deprecated and Fargate is the only supported option for new ECS deployments based on production deployment experience and this approach is commonly used in enterprise settings',
      'Fargate is only suitable for development environments and EC2 is required for production workloads following common design principles using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'Fargate vs EC2 decision: Choose Fargate when: (1) Variable/spiky workloads (pay per task). (2) Small team, want to minimize ops. (3) Rapid scaling requirements. (4) Short-lived tasks/batch jobs. (5) Security: task isolation by default. Choose EC2 when: (1) Steady-state workloads (EC2 RI/Savings Plans cheaper). (2) GPU requirements (P3, G4 instances). (3) Need specific instance types/features. (4) Large scale: manage capacity more granularly. (5) Windows containers (Fargate Windows has limitations). Hybrid: use both with capacity providers.',
    tags: ['aws', 'fargate', 'ecs', 'ec2', 'decision'],
  },

  // ─── ADDITIONAL ROUTE 53 QUESTIONS ────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What record types does Route 53 support?',
    options: [
      'A, AAAA, CNAME, MX, TXT, NS, SOA, SRV, CAA, and AWS-specific Alias records',
      'Route 53 only supports A records and CNAME records for basic DNS functionality',
      'Record types are automatically determined by Route 53 and cannot be manually configured',
      'MX and TXT records require a separate AWS service called Simple Email Service',
    ],
    correctIndex: 0,
    explanation: 'Route 53 record types: (1) A: IPv4 address. (2) AAAA: IPv6 address. (3) CNAME: canonical name (alias to another domain). (4) MX: mail exchange servers. (5) TXT: text records (SPF, DKIM, domain verification). (6) NS: name servers. (7) SOA: start of authority. (8) SRV: service locator. (9) CAA: certificate authority authorization. (10) Alias: AWS extension, points to AWS resources (CloudFront, ALB, S3), free, works at zone apex. NAPTR and PTR also supported.',
    tags: ['aws', 'route53', 'dns', 'record-types'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does Route 53 health checking work?',
    options: [
      'Health checkers worldwide monitor endpoints; unhealthy endpoints removed from DNS responses; integrates with failover routing',
      'Health checks only work with EC2 instances and cannot monitor external URLs or other AWS services as recommended for high availability which follows AWS recommended architecture patterns',
      'Route 53 health checks run once per day and require manual triggering for immediate status updates as documented in official AWS best practices',
      'Health checking is only available for private hosted zones and not public DNS records according to industry standard guidelines using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'Route 53 health checks: (1) Endpoint monitoring: HTTP, HTTPS, TCP checks from multiple global locations. (2) Intervals: 10 or 30 seconds. (3) Threshold: configurable failure count before unhealthy. (4) String matching: verify response contains expected text. (5) CloudWatch integration: alarms on health check status. (6) Calculated health checks: combine multiple checks with AND/OR. (7) Failover routing: automatically remove unhealthy endpoints from DNS. (8) Cost: ~$0.50/month per health check. Use for: active-passive failover, multi-region architectures.',
    tags: ['aws', 'route53', 'health-check', 'monitoring', 'failover'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is a private hosted zone in Route 53?',
    options: [
      'DNS zone that resolves only within associated VPCs; for internal service discovery without public DNS exposure',
      'Private hosted zones are encrypted versions of public zones with the same records and routing and this approach is commonly used in enterprise settings',
      'A zone that requires authentication before DNS queries are resolved for security purposes as documented in official AWS best practices',
      'Private zones only work with on-premises DNS servers through Direct Connect exclusively based on production deployment experience',
    ],
    correctIndex: 0,
    explanation: 'Private hosted zones: (1) DNS resolution only within associated VPCs. (2) Not accessible from internet. (3) Use same domain as public zone (split-horizon DNS). (4) Associate with multiple VPCs (same or cross-account). (5) Use cases: internal service discovery (db.internal), private APIs, microservices communication. (6) Enable DNS hostnames and DNS resolution in VPC settings. (7) Combine with Route 53 Resolver for hybrid DNS (on-premises integration). Example: app.internal → internal ALB, same app.example.com publicly → external ALB.',
    tags: ['aws', 'route53', 'private-hosted-zone', 'vpc', 'internal-dns'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'How do you implement active-passive failover with Route 53?',
    options: [
      'Create primary and secondary records with failover routing policy; attach health check to primary; traffic goes to secondary when primary unhealthy',
      'Active-passive failover requires two separate Route 53 hosted zones with manual switching based on standard operational procedures and this approach is commonly used in enterprise settings',
      'Failover routing only works with CloudFront distributions and not with EC2 or ALB endpoints according to industry standard guidelines following typical deployment workflows',
      'Route 53 does not support failover and requires Global Accelerator for any failover scenarios and this approach is commonly used in enterprise settings using conventional setup approaches',
    ],
    correctIndex: 0,
    explanation: 'Route 53 active-passive failover: (1) Create failover record set. (2) Primary record: points to main resource, associate health check. (3) Secondary record: points to standby/DR resource. (4) When primary health check fails, Route 53 returns secondary IP. (5) Health check: monitor primary endpoint (HTTP/HTTPS/TCP). (6) TTL consideration: lower TTL = faster failover, more DNS queries. (7) Use cases: DR site activation, maintenance windows. (8) Combine with CloudWatch alarms for alerting. Active-active: use weighted or latency routing instead.',
    tags: ['aws', 'route53', 'failover', 'disaster-recovery', 'high-availability'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'How do you do blue-green deployments using Route 53 weighted routing?',
    options: [
      'Create weighted records for blue and green environments; shift weight gradually (90/10 → 50/50 → 0/100) to migrate traffic',
      'Blue-green deployments require deleting and recreating DNS records for each deployment using conventional setup approaches based on production deployment experience',
      'Weighted routing only supports two endpoints maximum and cannot be used for gradual rollouts implementing typical operational patterns',
      'Route 53 weighted routing adds significant latency and should not be used for production traffic using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'Blue-green with weighted routing: (1) Blue (current): weighted record with weight 100. (2) Green (new): weighted record with weight 0. (3) Gradual shift: 90/10 → 70/30 → 50/50 → 0/100. (4) Monitor metrics/errors at each stage. (5) Rollback: shift weight back to blue. (6) TTL: use low TTL (60s) for faster shifts. (7) Complete cutover: delete blue record or set weight to 0. Alternative: use ALB with target groups for faster, more granular traffic shifting. Route 53 weighted routing is DNS-level, cached by resolvers.',
    tags: ['aws', 'route53', 'blue-green', 'weighted-routing', 'deployment'],
  },

  // ─── ADDITIONAL RDS QUESTIONS ─────────────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between RDS Multi-AZ and Read Replicas?',
    options: [
      'Multi-AZ: synchronous standby for HA, automatic failover. Read Replicas: async copies for read scaling, manual promotion',
      'Multi-AZ and Read Replicas are identical features with different names in the RDS console following typical deployment workflows following typical deployment workflows',
      'Read Replicas provide higher availability than Multi-AZ because they can be in different regions based on standard operational procedures',
      'Multi-AZ is for read scaling while Read Replicas are only for disaster recovery purposes which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'Multi-AZ vs Read Replicas: (1) Multi-AZ: synchronous replication to standby in different AZ, automatic failover (1-2 min), same endpoint, for high availability, standby not readable. (2) Read Replicas: asynchronous replication, separate endpoint, for read scaling, can be cross-region, manually promotable to standalone. (3) Multi-AZ + Read Replicas: use both for HA + read scaling. (4) Failover triggers: AZ outage, instance failure, storage failure, patching. (5) Read Replica uses: reporting queries, analytics, geographic distribution.',
    tags: ['aws', 'rds', 'multi-az', 'read-replica', 'high-availability'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is RDS Proxy and why would you use it?',
    options: [
      'Managed connection pooler that reduces database connections, improves failover time, and integrates with IAM/Secrets Manager',
      'RDS Proxy is a caching layer similar to ElastiCache that stores query results for faster access following common design principles as suggested by cloud architects',
      'Proxy is required for all RDS connections and is automatically enabled on all RDS instances as recommended for high availability which follows AWS recommended architecture patterns',
      'RDS Proxy only works with Aurora and is not compatible with MySQL or PostgreSQL RDS instances using standard configuration methods using established implementation patterns',
    ],
    correctIndex: 0,
    explanation: 'RDS Proxy benefits: (1) Connection pooling: multiplexes many application connections to fewer database connections. Crucial for Lambda (many short connections). (2) Faster failover: maintains connections during Multi-AZ failover (66% faster). (3) IAM authentication: apps authenticate with IAM, proxy handles DB credentials. (4) Secrets Manager integration: auto-rotates credentials. (5) Supported: MySQL, PostgreSQL, MariaDB, SQL Server (Aurora and RDS). (6) Use cases: serverless apps with Lambda, applications with connection exhaustion, improved security posture.',
    tags: ['aws', 'rds', 'rds-proxy', 'connection-pooling', 'lambda'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is Amazon Aurora and how is it different from standard RDS?',
    options: [
      'Aurora is AWS cloud-native database with 5x MySQL/3x PostgreSQL performance, auto-scaling storage, and up to 15 read replicas',
      'Aurora is identical to RDS MySQL but with a different pricing model and no additional features using conventional setup approaches as recommended for high availability',
      'Aurora only supports PostgreSQL and is not compatible with MySQL workloads or drivers following common design principles using standard configuration methods',
      'Aurora requires minimum 3 instances running at all times and cannot scale down to reduce costs following the recommended security practices',
    ],
    correctIndex: 0,
    explanation: 'Aurora vs RDS: (1) Performance: 5x MySQL, 3x PostgreSQL throughput. (2) Storage: auto-scales 10GB to 128TB, replicated 6 ways across 3 AZs. (3) Replicas: up to 15 read replicas with <10ms lag (vs 5 for RDS). (4) Failover: <30 seconds. (5) Serverless: Aurora Serverless v2 auto-scales compute. (6) Global Database: <1 second replication to 5 regions. (7) Backtrack: rewind database to point in time without restore. (8) Cost: ~20% more than RDS but better performance/availability. Choose Aurora for: high performance, high availability, global scale.',
    tags: ['aws', 'aurora', 'rds', 'database', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'Application cannot connect to RDS. How do you troubleshoot?',
    options: [
      'Check: security groups, subnet/route tables, credentials, RDS status, DNS resolution, SSL requirements, max connections',
      'RDS connection issues are always caused by incorrect passwords and require resetting the master password as recommended for high availability',
      'Restart the RDS instance as this is the only way to resolve connectivity issues following typical deployment workflows which follows AWS recommended architecture patterns',
      'Connection failures indicate the RDS instance type is too small and must be upgraded following typical deployment workflows as recommended for high availability',
    ],
    correctIndex: 0,
    explanation: 'RDS connectivity troubleshooting: (1) Security groups: RDS SG allows inbound on DB port (3306/5432) from app SG/IP. (2) Network: app and RDS in same VPC, or VPC peering/Transit Gateway configured. (3) Route tables: proper routing between subnets. (4) Credentials: verify username, password, database name. (5) Endpoint: use correct RDS endpoint (not IP). (6) DNS: VPC DNS resolution enabled. (7) SSL: if required, verify certificate configuration. (8) Max connections: check if limit reached (CloudWatch). (9) RDS status: verify instance is "available". (10) Test: nc -zv endpoint port.',
    tags: ['aws', 'rds', 'connectivity', 'troubleshooting', 'networking'],
  },

  // ─── ADDITIONAL CLOUDFRONT QUESTIONS ──────────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are CloudFront behaviors and how do you use them?',
    options: [
      'Behaviors define how CloudFront handles requests for different path patterns; configure caching, origin, protocol per path',
      'Behaviors are CloudFront logging settings that control which requests are recorded in access logs which follows AWS recommended architecture patterns',
      'CloudFront behaviors are identical to Route 53 routing policies and serve the same purpose following common design principles as documented in official AWS best practices',
      'Behaviors only apply to S3 origins and cannot be configured for ALB or custom origins following common design principles according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'CloudFront behaviors: (1) Path pattern: /api/*, /images/*, default (*). (2) Each behavior configures: origin, cache policy, origin request policy, viewer protocol, allowed methods. (3) Priority order: most specific path first. (4) Use cases: /api/* → ALB origin (no caching), /static/* → S3 (long cache), /* → S3 (default). (5) Cache policies: control TTL, headers/cookies/query strings in cache key. (6) Origin request policies: what to forward to origin. (7) Function associations: CloudFront Functions or Lambda@Edge per behavior.',
    tags: ['aws', 'cloudfront', 'behaviors', 'caching', 'routing'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between signed URLs and signed cookies in CloudFront?',
    options: [
      'Signed URLs: access to single file with URL token. Signed cookies: access to multiple files without URL changes',
      'Signed URLs and cookies are identical and the choice is purely based on developer preference as recommended for high availability',
      'Signed cookies are more secure than signed URLs and should always be preferred for all use cases based on standard operational procedures',
      'Signed URLs only work with S3 origins while signed cookies work with any origin type and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Signed URLs vs Signed Cookies: (1) Signed URLs: access single file, token in URL, visible in logs, easy to share/revoke. Use for: individual file downloads, email links. (2) Signed cookies: access multiple files, set via Set-Cookie header, no URL changes, harder to share. Use for: streaming video (multiple segments), member areas, multiple related files. (3) Both use: key pairs (trusted key groups), expiration time, IP restrictions optional. (4) Canned vs custom policies: custom allows more conditions (date ranges, IP). Generate with AWS SDK or CLI.',
    tags: ['aws', 'cloudfront', 'signed-urls', 'signed-cookies', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you block direct access to your origin, ensuring all traffic goes through CloudFront?',
    options: [
      'For S3: use OAC. For ALB: use custom header that CloudFront adds, verify in ALB rules or WAF',
      'Direct origin access cannot be blocked and users can always bypass CloudFront if they know the origin URL',
      'CloudFront automatically blocks all direct origin access without any configuration needed',
      'Origin access blocking requires AWS Shield Advanced subscription on all origin resources',
    ],
    correctIndex: 0,
    explanation: 'Block direct origin access: (1) S3 origin: Use Origin Access Control (OAC), bucket policy allows only CloudFront principal. (2) ALB/Custom origin: Add custom header in CloudFront (X-Origin-Verify: secret-value), configure ALB listener rule or WAF to require this header. (3) Security group: ALB SG allows only CloudFront IPs (AWS managed prefix list: com.amazonaws.global.cloudfront.origin-facing). (4) ALB internal: make ALB internal, use VPC origin in CloudFront. Benefits: enforce caching, WAF protection, hide origin, consistent security policy.',
    tags: ['aws', 'cloudfront', 'origin-security', 'oac', 'waf'],
  },

  // ─── ADDITIONAL ECS/CONTAINER QUESTIONS ───────────────────────────────────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the ECS container agent and what does it do?',
    options: [
      'Agent running on EC2 instances that registers instances with cluster, manages containers, reports metrics to ECS control plane',
      'Container agent is required for Fargate tasks and must be manually installed in each container image as documented in official AWS best practices',
      'ECS agent is a separate AWS service that costs additional fees for container monitoring using standard configuration methods according to industry standard guidelines',
      'Container agent is only needed for Windows containers and Linux containers run without an agent as suggested by cloud architects using standard configuration methods',
    ],
    correctIndex: 0,
    explanation: 'ECS container agent: (1) Runs on each EC2 instance in ECS cluster (not Fargate — serverless). (2) Communicates with ECS control plane. (3) Starts/stops containers based on task placement. (4) Reports container status, resource utilization. (5) Pre-installed on ECS-optimized AMIs. (6) Open source, can install on custom AMIs. (7) Requires IAM instance role (AmazonEC2ContainerServiceforEC2Role). (8) Configuration: /etc/ecs/ecs.config (cluster name, logging, attributes). Update: drain instance, update agent, register back.',
    tags: ['aws', 'ecs', 'container-agent', 'ec2-launch-type'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you pass secrets and environment variables to ECS containers?',
    options: [
      'Use Secrets Manager or Parameter Store with valueFrom in task definition; ECS agent retrieves secrets at runtime',
      'Secrets must be hardcoded in the Docker image during build time for ECS to access them following typical deployment workflows',
      'Environment variables can only be passed through Docker Compose files in ECS deployments using conventional setup approaches',
      'ECS does not support secrets and sensitive data must be stored in environment variables as plaintext as suggested by cloud architects',
    ],
    correctIndex: 0,
    explanation: 'ECS secrets management: (1) Secrets Manager: valueFrom: arn:aws:secretsmanager:region:account:secret:name. (2) Parameter Store: valueFrom: arn:aws:ssm:region:account:parameter/name. (3) Task execution role needs permissions (secretsmanager:GetSecretValue, ssm:GetParameters). (4) Secrets injected as environment variables at container start. (5) Plain environment variables: for non-sensitive config. (6) Benefits: secrets not in task definition JSON, automatic rotation support, audit trail. (7) Container-level granularity: different secrets per container in task.',
    tags: ['aws', 'ecs', 'secrets', 'environment-variables', 'secrets-manager'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does ECS service auto scaling work?',
    options: [
      'Application Auto Scaling adjusts desired task count based on CloudWatch metrics like CPU, memory, or custom metrics',
      'ECS services cannot auto-scale and always run a fixed number of tasks defined in the service configuration according to industry standard guidelines',
      'Auto scaling in ECS only works with EC2 launch type and is not available for Fargate services as suggested by cloud architects',
      'Service scaling requires manual API calls and cannot be triggered automatically by metrics following common design principles',
    ],
    correctIndex: 0,
    explanation: 'ECS Service Auto Scaling: (1) Uses Application Auto Scaling service. (2) Scaling policies: target tracking (maintain CPU at 70%), step scaling (scale +2 when CPU > 80%). (3) Metrics: ECSServiceAverageCPUUtilization, ECSServiceAverageMemoryUtilization, ALBRequestCountPerTarget, custom metrics. (4) Min/max capacity limits. (5) Scale-in protection: prevent scaling in during deployments. (6) Cooldown: prevent rapid scaling. (7) Works with both EC2 and Fargate. (8) EC2 launch type: also scale underlying instances with Cluster Auto Scaling (capacity providers).',
    tags: ['aws', 'ecs', 'auto-scaling', 'application-auto-scaling'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'ECS container keeps restarting. How do you investigate?',
    options: [
      'Check CloudWatch Logs for application errors, verify health check configuration, review exit codes, check resource limits',
      'Container restarts are normal ECS behavior and indicate the service is functioning correctly according to industry standard guidelines',
      'Restarting containers can only be fixed by rebuilding the Docker image from scratch based on standard operational procedures based on standard operational procedures',
      'ECS does not provide any debugging tools and you must contact AWS support for container issues following typical deployment workflows',
    ],
    correctIndex: 0,
    explanation: 'ECS container restart troubleshooting: (1) CloudWatch Logs: check application logs for errors, exceptions, crashes. (2) Exit codes: stopped task shows exit code (137=OOM killed, 1=app error). (3) Health check: container failing health check → restart. Verify path, interval, timeout, retries. (4) Resource limits: CPU/memory too low → container killed. Check container insights. (5) Dependency: depends_on containers failing. (6) Startup: application not starting properly within startPeriod. (7) Credentials: secrets failing to retrieve. (8) ECS Exec: enable and shell into running container to debug live.',
    tags: ['aws', 'ecs', 'container', 'troubleshooting', 'restart'],
  },

  // ─── FINAL BATCH: ALB, API GW, ASG, CLOUDFRONT, EVENTBRIDGE, EBS, EFS ─────────
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do health checks work on an ALB?',
    options: [
      'ALB sends HTTP/HTTPS requests to targets at configured intervals; targets must return 200-399 status codes to be considered healthy',
      'Health checks only verify that the EC2 instance is running, not that the application is responding correctly and this approach is commonly used in enterprise settings',
      'ALB health checks are optional and disabled by default for all target groups based on production deployment experience as documented in official AWS best practices',
      'Health checks run once per hour and cannot be configured to run more frequently based on production deployment experience as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'ALB health checks: (1) Protocol: HTTP, HTTPS, or TCP. (2) Path: e.g., /health or /api/status. (3) Interval: how often to check (default 30s). (4) Timeout: how long to wait for response (default 5s). (5) Healthy threshold: consecutive successes to mark healthy (default 5). (6) Unhealthy threshold: consecutive failures to mark unhealthy (default 2). (7) Success codes: 200-399 by default, configurable. (8) Unhealthy targets: ALB stops sending traffic until healthy. Configure in target group settings.',
    tags: ['aws', 'alb', 'health-check', 'target-group'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is sticky sessions (session affinity) in ALB and when would you use it?',
    options: [
      'Sticky sessions route requests from the same client to the same target using cookies; use for stateful applications that store session data locally',
      'Sticky sessions encrypt all traffic between the client and ALB for enhanced security purposes according to industry standard guidelines which follows AWS recommended architecture patterns',
      'Session affinity is required for all ALB configurations and cannot be disabled once enabled and this approach is commonly used in enterprise settings which follows AWS recommended architecture patterns',
      'Sticky sessions only work with WebSocket connections and not with standard HTTP requests based on production deployment experience as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'Sticky sessions (session affinity): (1) Routes all requests from a client to the same target. (2) Implementation: ALB generates AWSALB cookie or uses application cookie. (3) Duration: configurable (1 second to 7 days). (4) Use cases: applications storing session state locally, shopping carts, user preferences not in shared store. (5) Drawbacks: uneven load distribution, target failure loses session. (6) Better approach: externalize session state to ElastiCache/DynamoDB. (7) Configure: target group attributes → stickiness.enabled = true.',
    tags: ['aws', 'alb', 'sticky-sessions', 'session-affinity'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you route /api/* to ECS and /* to CloudFront using one ALB?',
    options: [
      'Create two target groups and ALB listener rules: /api/* routes to ECS target group, default rule routes to CloudFront origin or S3',
      'ALB cannot route to CloudFront directly; you need two separate ALBs for this architecture as documented in official AWS best practices and this approach is commonly used in enterprise settings',
      'Use Route 53 weighted routing instead of ALB path-based routing for this use case as documented in official AWS best practices based on production deployment experience',
      'ALB only supports one target group per listener and cannot do path-based routing as documented in official AWS best practices according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'ALB path-based routing setup: (1) Create target groups: ECS-TG for API containers, Static-TG for static content. (2) For CloudFront origin: ALB routes to S3/static server, CloudFront sits in front of entire ALB. (3) ALB listener rules: Rule 1: path=/api/* → forward to ECS-TG. Rule 2: default → forward to Static-TG. (4) Priority: /api/* rule higher priority than default. (5) Alternative architecture: CloudFront with multiple origins — ALB origin for /api/*, S3 origin for /*. This is often cleaner than ALB routing to CloudFront.',
    tags: ['aws', 'alb', 'path-routing', 'cloudfront', 'architecture'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'How does API Gateway integrate with Lambda?',
    options: [
      'API Gateway invokes Lambda synchronously, passing request data as event; Lambda returns response that API Gateway sends to client',
      'Lambda must poll API Gateway for incoming requests using long-polling mechanism according to industry standard guidelines as documented in official AWS best practices',
      'API Gateway and Lambda require an SQS queue between them for all communications as documented in official AWS best practices according to industry standard guidelines',
      'Lambda functions must be deployed in the same region as API Gateway REST APIs only based on production deployment experience and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'API Gateway + Lambda integration: (1) Proxy integration (recommended): API Gateway passes entire request (headers, body, path, query) as Lambda event. Lambda returns statusCode, headers, body. (2) Non-proxy integration: map request/response with templates (VTL). (3) Invocation: synchronous by default. (4) Permissions: API Gateway needs lambda:InvokeFunction permission (resource-based policy). (5) Timeout: API Gateway max 29 seconds, Lambda max 15 minutes. (6) Payload: 10MB max for REST API, 6MB for Lambda response.',
    tags: ['aws', 'api-gateway', 'lambda', 'integration'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you cache API responses in API Gateway?',
    options: [
      'Enable caching on a stage, configure TTL and cache size; API Gateway caches responses by cache key (URL, headers, query params)',
      'Caching is automatic in API Gateway and cannot be configured or disabled by users and this approach is commonly used in enterprise settings',
      'API Gateway does not support caching; use CloudFront in front of API Gateway instead and this approach is commonly used in enterprise settings',
      'Caching only works with HTTP APIs and is not available for REST APIs based on production deployment experience as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'API Gateway caching (REST API only): (1) Enable per stage with cache capacity (0.5GB to 237GB). (2) TTL: 0 to 3600 seconds (default 300). (3) Cache key: method, resource path, query strings, headers you specify. (4) Cache invalidation: Cache-Control: max-age=0 header, or flush via console/API. (5) Cost: pay for cache capacity per hour. (6) Per-method override: enable/disable caching for specific methods. (7) Note: HTTP API does not support caching — use CloudFront. Use caching for: read-heavy APIs, expensive backend calls, static or slowly changing data.',
    tags: ['aws', 'api-gateway', 'caching', 'performance'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do you secure an API Gateway endpoint?',
    options: [
      'Use IAM authorization, Lambda/Cognito authorizers, API keys with usage plans, resource policies, WAF, and mutual TLS',
      'API Gateway endpoints are secure by default and require no additional configuration based on production deployment experience as documented in official AWS best practices',
      'Security is only available for private APIs and public APIs cannot be secured and this approach is commonly used in enterprise settings',
      'API Gateway security requires AWS Shield Advanced subscription for all endpoints and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'API Gateway security options: (1) IAM authorization: sigv4 signed requests, for AWS service/CLI access. (2) Lambda authorizer: custom auth logic (JWT validation, database lookup). (3) Cognito authorizer: validate JWT tokens from Cognito User Pools. (4) API keys + usage plans: rate limiting per client, not for authentication. (5) Resource policies: IP allowlist, VPC endpoint restrictions. (6) WAF: protect against SQL injection, XSS, rate-based rules. (7) Mutual TLS: client certificate authentication. (8) Private API: accessible only from VPC via endpoint.',
    tags: ['aws', 'api-gateway', 'security', 'authorization'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is an Auto Scaling Group (ASG)?',
    options: [
      'ASG automatically adjusts EC2 instance count based on demand, health checks, and scaling policies to maintain availability and optimize costs',
      'ASG is a security feature that groups EC2 instances with the same security group settings which follows AWS recommended architecture patterns according to industry standard guidelines',
      'Auto Scaling Group is another name for an EC2 placement group with spread strategy according to industry standard guidelines which follows AWS recommended architecture patterns',
      'ASG only works with Spot Instances and cannot manage On-Demand or Reserved Instances based on production deployment experience and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'Auto Scaling Group: (1) Maintains desired number of EC2 instances. (2) Launches/terminates instances based on scaling policies. (3) Replaces unhealthy instances automatically. (4) Distributes instances across AZs. (5) Components: launch template (instance config), min/max/desired capacity, scaling policies, health checks. (6) Integration: ELB registration, CloudWatch metrics, lifecycle hooks. (7) Use cases: handle variable load, maintain high availability, cost optimization. (8) Scaling: manual, scheduled, dynamic (target tracking, step, simple).',
    tags: ['aws', 'auto-scaling', 'asg', 'fundamentals'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does ASG handle unhealthy instances?',
    options: [
      'ASG terminates unhealthy instances and launches replacements to maintain desired capacity; uses EC2 status checks and/or ELB health checks',
      'Unhealthy instances are kept running but removed from the load balancer until manually fixed based on production deployment experience according to industry standard guidelines',
      'ASG sends an alert but takes no automatic action on unhealthy instances according to industry standard guidelines as documented in official AWS best practices',
      'Health checks only run once when instances launch, not continuously which follows AWS recommended architecture patterns according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'ASG health check behavior: (1) Health check types: EC2 (default) — instance status checks; ELB — target group health. (2) Grace period: time after launch before health checks start (default 300s). (3) Unhealthy detection: instance fails health check. (4) Action: ASG terminates unhealthy instance, launches replacement. (5) ELB health check: more thorough, checks application health. (6) Custom health checks: set instance health via API. (7) Best practice: use ELB health checks for production. (8) Note: terminating instance counts toward desired capacity — replacement launches immediately.',
    tags: ['aws', 'auto-scaling', 'health-check', 'instance-management'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'How do you do a rolling update of EC2 instances in an ASG?',
    options: [
      'Use instance refresh: ASG replaces instances in batches while maintaining minimum healthy percentage and honoring warm-up time',
      'Delete the ASG and create a new one with updated launch template; there is no in-place update mechanism according to industry standard guidelines',
      'Manually terminate instances one by one and let ASG launch replacements with new configuration which follows AWS recommended architecture patterns',
      'Rolling updates require CloudFormation and cannot be done through ASG directly based on production deployment experience which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'ASG Instance Refresh: (1) Update launch template with new AMI/config. (2) Start instance refresh via console/CLI. (3) Parameters: min healthy percentage (e.g., 90%), instance warmup (e.g., 300s). (4) Process: ASG terminates batch of old instances, launches new ones, waits for warmup, repeats. (5) Checkpoints: pause at percentage intervals for validation. (6) Rollback: cancel refresh to stop (already replaced instances remain). (7) Alternative: CloudFormation UpdatePolicy with AutoScalingRollingUpdate. (8) Blue-green: create new ASG, shift ALB traffic, delete old ASG.',
    tags: ['aws', 'auto-scaling', 'rolling-update', 'instance-refresh'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What is the difference between CloudFront and S3 website hosting?',
    options: [
      'CloudFront is a CDN caching content at edge locations globally; S3 website hosting serves files directly from one region without caching or edge distribution',
      'S3 website hosting is faster than CloudFront because files are served directly without CDN overhead as documented in official AWS best practices according to industry standard guidelines',
      'CloudFront and S3 website hosting are identical features with different pricing models based on production deployment experience as documented in official AWS best practices',
      'S3 website hosting supports HTTPS with custom domains while CloudFront does not as documented in official AWS best practices according to industry standard guidelines as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'CloudFront vs S3 website hosting: (1) S3 website hosting: serves static files from single region, HTTP only with S3 domain (HTTPS requires CloudFront), no caching, simple setup. (2) CloudFront: global CDN with 400+ edge locations, caches content close to users, HTTPS with custom domains via ACM, supports S3 and other origins, WAF integration, signed URLs. (3) Performance: CloudFront faster for global users (edge caching). (4) Cost: S3 cheaper for low traffic; CloudFront cost-effective at scale. (5) Best practice: use CloudFront in front of S3 for production websites.',
    tags: ['aws', 'cloudfront', 's3', 'website-hosting', 'comparison'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What are EventBridge rules and targets?',
    options: [
      'Rules match incoming events using patterns; targets are AWS services (Lambda, SQS, Step Functions) that receive matched events',
      'Rules and targets are the same concept in EventBridge terminology according to industry standard guidelines according to industry standard guidelines',
      'Targets define which events to capture; rules define where to send them according to industry standard guidelines as documented in official AWS best practices',
      'EventBridge rules only work with CloudWatch Events and not with custom events according to industry standard guidelines and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EventBridge rules and targets: (1) Rule: defines event pattern to match (source, detail-type, detail fields) or schedule expression (cron/rate). (2) Event pattern: {"source": ["aws.ec2"], "detail-type": ["EC2 Instance State-change Notification"]}. (3) Target: AWS service to invoke when rule matches — Lambda, SQS, SNS, Step Functions, Kinesis, ECS task, API Gateway, and more. (4) Up to 5 targets per rule. (5) Input transformation: modify event before sending to target. (6) DLQ: configure dead-letter queue for failed deliveries. (7) Use cases: event-driven automation, microservices decoupling, scheduled tasks.',
    tags: ['aws', 'eventbridge', 'rules', 'targets', 'event-driven'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is an EventBridge Pipe?',
    options: [
      'Pipes connect event sources directly to targets with optional filtering, enrichment, and transformation — point-to-point integration without rules',
      'Pipes are deprecated in favor of EventBridge rules and should not be used for new implementations based on production deployment experience as documented in official AWS best practices',
      'EventBridge Pipes only work with SQS as source and Lambda as target with no other combinations supported as documented in official AWS best practices based on production deployment experience',
      'Pipes are EventBridge logging mechanism that records all events for auditing purposes as documented in official AWS best practices which follows AWS recommended architecture patterns',
    ],
    correctIndex: 0,
    explanation: 'EventBridge Pipes: (1) Point-to-point integration connecting source to target. (2) Sources: SQS, Kinesis, DynamoDB Streams, Kafka, MQ. (3) Targets: Lambda, Step Functions, API Gateway, ECS, Kinesis, and 15+ services. (4) Filtering: filter events before processing (reduce costs). (5) Enrichment: call Lambda or API to add data to event. (6) Transformation: modify event structure before delivery. (7) Use case: simpler than rules for direct source-to-target flows. (8) Difference from rules: Pipes poll sources; rules match events on event bus. Pipes for streaming data; rules for event routing.',
    tags: ['aws', 'eventbridge', 'pipes', 'integration'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'Can an EBS volume be attached to multiple EC2 instances? (Multi-Attach)',
    options: [
      'Yes, Multi-Attach is supported for io1/io2 Provisioned IOPS volumes, allowing up to 16 instances in the same AZ to attach concurrently',
      'EBS volumes can be attached to unlimited instances across multiple Availability Zones simultaneously which follows AWS recommended architecture patterns',
      'Multi-Attach is available for all EBS volume types including gp2 and gp3 as documented in official AWS best practices according to industry standard guidelines',
      'EBS does not support Multi-Attach; use EFS for shared storage across instances and this approach is commonly used in enterprise settings as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'EBS Multi-Attach: (1) Only for io1/io2 Provisioned IOPS SSD volumes. (2) Up to 16 Linux instances in same AZ. (3) Use cases: clustered applications (Oracle RAC), high-availability file systems requiring shared block storage. (4) Requirements: cluster-aware filesystem (not ext4/xfs), application handles concurrent writes. (5) Limitations: same AZ only, NITRO-based instances only, not for boot volumes. (6) Alternative: EFS for shared file storage (simpler, NFS-based). (7) Multi-Attach is specialized — most use cases better served by EFS or application-level replication.',
    tags: ['aws', 'ebs', 'multi-attach', 'shared-storage'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How do EBS snapshots work? Can you copy them across regions?',
    options: [
      'Snapshots are incremental backups stored in S3; yes, you can copy snapshots to other regions for DR or migration',
      'Snapshots are full backups each time and cannot be copied across regions due to data residency requirements according to industry standard guidelines',
      'EBS snapshots are stored on the same volume and deleted when the volume is deleted according to industry standard guidelines',
      'Cross-region snapshot copy requires AWS Backup service and cannot be done directly and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EBS snapshots: (1) Point-in-time backup stored in S3 (managed by AWS). (2) Incremental: only changed blocks since last snapshot are stored. (3) First snapshot: full copy. Subsequent: only changes. (4) Cross-region copy: yes, for DR, migration, or sharing. (5) Cross-account sharing: modify snapshot permissions. (6) Encryption: can encrypt unencrypted snapshot during copy. (7) Restore: create new volume from snapshot in any AZ. (8) Automation: AWS Backup, Data Lifecycle Manager for scheduled snapshots. (9) Cost: pay for stored data, plus data transfer for cross-region.',
    tags: ['aws', 'ebs', 'snapshots', 'backup', 'cross-region'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'How does EBS encryption work?',
    options: [
      'EBS uses AWS KMS keys to encrypt data at rest, in transit to EC2, and all snapshots; encryption is transparent to applications',
      'EBS encryption only protects data at rest and requires separate TLS configuration for in-transit encryption which follows AWS recommended architecture patterns',
      'Encryption must be implemented at the application level; EBS does not provide native encryption based on production deployment experience',
      'EBS encryption requires third-party encryption software installed on EC2 instances and this approach is commonly used in enterprise settings',
    ],
    correctIndex: 0,
    explanation: 'EBS encryption: (1) Uses AWS KMS (CMK or AWS managed key). (2) Encrypts: data at rest on volume, data in transit between EC2 and EBS, all snapshots, volumes created from encrypted snapshots. (3) Transparent: no application changes, minimal latency impact. (4) Enable: at volume creation or by copying unencrypted snapshot with encryption. (5) Default encryption: enable per-region to encrypt all new volumes. (6) Cannot: encrypt existing unencrypted volume in-place (copy via snapshot). (7) Instance types: all current generation support encryption. (8) Key management: rotate KMS keys, audit via CloudTrail.',
    tags: ['aws', 'ebs', 'encryption', 'kms', 'security'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'EC2 disk is full. What are your options without data loss?',
    options: [
      'Extend EBS volume size online, then extend filesystem; or identify and delete unnecessary files; or add additional EBS volume',
      'Terminate the instance and launch a new one with a larger root volume to recover the data according to industry standard guidelines which follows AWS recommended architecture patterns',
      'EBS volumes cannot be resized and the only option is to restore from a backup to a larger volume which follows AWS recommended architecture patterns',
      'Stop the instance, delete the volume, and restore from the latest snapshot to a larger volume as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'Disk full solutions (no data loss): (1) Extend EBS volume: modify volume size via console/CLI (online, no downtime). Then extend filesystem: resize2fs /dev/xvda1 (ext4) or xfs_growfs / (xfs). (2) Clean up: find large files (du -sh /*, find / -size +100M), clear logs, remove unused packages. (3) Add volume: attach new EBS, format, mount, move data. (4) Use LVM: if configured, extend logical volume. (5) Move data to S3/EFS: offload infrequently accessed data. (6) Prevention: CloudWatch alarm on disk usage, auto-extend scripts, proper log rotation.',
    tags: ['aws', 'ebs', 'disk-full', 'troubleshooting', 'resize'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'SCENARIO' as const,
    question: 'How do you migrate an EBS volume to a different AZ?',
    options: [
      'Create snapshot of the volume, then create new volume from snapshot in the target AZ',
      'Use AWS DataSync to directly copy the EBS volume to another AZ',
      'EBS volumes can be moved between AZs using the ModifyVolume API with target AZ parameter',
      'Detach the volume and reattach it to an instance in the new AZ; volumes are AZ-independent',
    ],
    correctIndex: 0,
    explanation: 'EBS volume AZ migration: (1) EBS volumes are AZ-specific and cannot be directly moved. (2) Process: create snapshot (can be done while attached), create new volume from snapshot in target AZ, attach to instance in target AZ. (3) Downtime consideration: for consistency, stop writes or unmount before snapshot. (4) Alternative: use AMI — create AMI from instance, launch new instance in target AZ. (5) Cross-region: copy snapshot to target region first, then create volume. (6) Automation: AWS Backup, Lambda scripts for scheduled migrations.',
    tags: ['aws', 'ebs', 'migration', 'availability-zone', 'snapshot'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'What access modes does EFS support?',
    options: [
      'EFS supports multiple EC2 instances reading and writing simultaneously with POSIX-compliant file locking',
      'EFS only supports read-only access from multiple instances; writes require exclusive mount according to industry standard guidelines',
      'EFS supports single-instance access only, similar to EBS volume attachment based on production deployment experience',
      'Access modes must be configured per file; EFS does not have system-wide access settings according to industry standard guidelines',
    ],
    correctIndex: 0,
    explanation: 'EFS access modes: (1) Multiple instances: thousands of EC2/containers can mount simultaneously. (2) Read/write: all clients can read and write concurrently. (3) POSIX permissions: standard Unix file permissions (chmod, chown). (4) File locking: advisory locks supported for coordination. (5) NFS protocol: NFSv4.1. (6) Access points: create different entry points with different permissions/paths. (7) IAM authorization: control mount permissions with IAM policies. (8) Network: accessible from all AZs in the region via mount targets.',
    tags: ['aws', 'efs', 'access-modes', 'permissions'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'EASY' as const,
    type: 'THEORY' as const,
    question: 'Can EFS be shared across AZs?',
    options: [
      'Yes, EFS is regional and can be mounted from any AZ in the same region via mount targets in each AZ',
      'EFS is AZ-specific like EBS and requires separate file systems per AZ based on production deployment experience',
      'Cross-AZ access requires AWS DataSync to replicate data between AZ-specific EFS file systems according to industry standard guidelines',
      'EFS only works within a single VPC and cannot span multiple Availability Zones as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'EFS cross-AZ access: (1) EFS is regional service — single filesystem spans all AZs. (2) Mount targets: create one in each AZ where you need access. (3) Instances connect to mount target in their AZ (low latency). (4) Data automatically replicated across AZs by AWS. (5) High availability: survives AZ failure. (6) Cross-VPC: use VPC peering or Transit Gateway. (7) Cross-region: use AWS DataSync for replication (not native). (8) One Zone storage class: optional single-AZ for cost savings (20% cheaper).',
    tags: ['aws', 'efs', 'availability-zone', 'regional', 'high-availability'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'MEDIUM' as const,
    type: 'THEORY' as const,
    question: 'What is EFS Infrequent Access (IA)?',
    options: [
      'Lower-cost storage class for files not accessed frequently; lifecycle policies automatically move files to IA based on access patterns',
      'EFS IA is a separate EFS product that must be provisioned independently from standard EFS which follows AWS recommended architecture patterns and this approach is commonly used in enterprise settings',
      'Infrequent Access mode reduces EFS performance to save costs and requires manual file placement based on production deployment experience according to industry standard guidelines',
      'IA only works with EFS One Zone storage class and is not available for standard EFS according to industry standard guidelines as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'EFS Infrequent Access: (1) Storage class for files not accessed for N days (configurable: 7, 14, 30, 60, 90). (2) Cost: up to 92% lower storage cost than Standard. (3) Lifecycle policy: automatically transitions files to IA. (4) Access: higher per-read cost, but files immediately accessible (no retrieval delay). (5) Intelligent-Tiering: automatically moves back to Standard on access. (6) Use case: logs, backups, infrequently accessed data. (7) Configure: enable lifecycle management on filesystem. (8) One Zone IA: single-AZ variant for even lower cost (non-critical data).',
    tags: ['aws', 'efs', 'infrequent-access', 'cost-optimization', 'storage-class'],
  },
  {
    topicSlug: 'aws-advanced',
    difficulty: 'HARD' as const,
    type: 'SCENARIO' as const,
    question: 'EFS mount fails. How do you troubleshoot?',
    options: [
      'Check: security groups allow NFS port 2049, mount target exists in AZ, DNS resolution works, IAM permissions if using IAM auth',
      'EFS mount failures are always caused by incorrect file system ID and require creating a new filesystem based on production deployment experience',
      'Run aws efs repair-mount command to automatically diagnose and fix all mount issues which follows AWS recommended architecture patterns according to industry standard guidelines',
      'EFS mounts are managed by AWS and troubleshooting requires opening a support ticket according to industry standard guidelines as documented in official AWS best practices',
    ],
    correctIndex: 0,
    explanation: 'EFS mount troubleshooting: (1) Security groups: EFS mount target SG must allow inbound TCP 2049 from instance SG. (2) Mount target: exists in instance AZ? Check VPC and subnet. (3) DNS: fs-xxx.efs.region.amazonaws.com resolves? (needs VPC DNS enabled). (4) NFS utils: amazon-efs-utils installed? (5) IAM: if using IAM authorization, check policy allows elasticfilesystem:ClientMount. (6) Network: instance can reach mount target (route table, NACL). (7) Test: nc -zv mount-target-ip 2049. (8) Logs: /var/log/amazon/efs for EFS mount helper. (9) Timeout: often security group or network issue.',
    tags: ['aws', 'efs', 'mount', 'troubleshooting', 'networking'],
  },
];
